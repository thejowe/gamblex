# Lobby de selección de juego — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sustituir las 7 mesas apiladas verticalmente en `CasinoFloor` por
un lobby con rejilla de 7 tarjetas de juego; al pulsar una, esa mesa ocupa
toda la pantalla de ESE cliente (las demás quedan ocultas para él) con un
botón "Volver al lobby"; el HUD de meta colectiva/batalla sigue visible
tanto en el lobby como dentro de cualquier sala.

**Architecture:** Cambio puramente de cliente/UI sobre `scenes/casino_floor.tscn`
y `scripts/net/casino_floor.gd`. Las 7 mesas (`*TableNet`) ya son hijas de
`CasinoFloor`; en vez de posiciones apiladas y `visible=true` fijo, cada una
ocupa el rectángulo completo de pantalla (`anchors_preset=15`) y arranca
oculta. Un nuevo nodo `Lobby` (rejilla de 7 botones-tarjeta) decide qué mesa
se muestra. La decisión de qué mostrar vive en una clase pura y testeable
(`LobbyController`, sin dependencias de red ni de escena) que
`casino_floor.gd` usa para alternar `visible` — mismo patrón que
`CollectiveGoal`/`MatchRules` en este repo: lógica aislada + wiring fino en
el script de escena. El HUD (`GoalLabel`/`UnlockedBanner`/`BattleStatusLabel`)
se muda a un `CanvasLayer` (`Hud`) para quedar siempre dibujado por encima
del lobby o de cualquier mesa, sin tocar la lógica que ya lo alimenta.

**Tech Stack:** Godot 4 (GDScript), GUT (`addons/gut`) para tests unitarios.

**Spec:** `docs/superpowers/specs/2026-08-17-casino-multiplayer-design.md`
(sección "Ampliación v1.2: Lobby de selección de juego").

## Global Constraints

- No se toca `GameLogic`/`TableState`/`TableController` de ningún juego, ni
  cómo se conectan los RPCs — cambio 100% de cliente/UI.
- No hay sincronización de "en qué sala está cada jugador" entre clientes —
  decisión 100% local, nunca se envía por red.
- El HUD (`GoalLabel`/`UnlockedBanner` en modo libre, `BattleStatusLabel` en
  modo batalla) debe seguir visible tanto en el lobby como dentro de
  cualquier sala — nunca se oculta al entrar a una mesa.
- Los 7 nodos de mesa (`BlackjackTableNet`, `RouletteTableNet`,
  `PokerTableNet`, `DiceTableNet`, `CrashTableNet`, `MinesTableNet`,
  `PlinkoTableNet`) son exactamente los nombres ya usados en
  `scenes/casino_floor.tscn` — no se renombran.
- Comando de test headless: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`

---

## Task 1: `LobbyController` — lógica pura de navegación lobby/sala

**Files:**
- Create: `scripts/lobby/lobby_controller.gd`
- Test: `tests/unit/test_lobby_controller.gd`

**Interfaces:**
- Produces: clase global `LobbyController` (via `class_name`, `extends
  RefCounted`) con:
  - `LobbyController.new()` — arranca en el lobby (ninguna mesa activa).
  - `is_in_lobby() -> bool`
  - `select(id: String) -> void` — activa la mesa `id`, sale del lobby.
  - `return_to_lobby() -> void` — vuelve al lobby, ninguna mesa activa.
  - `is_active(id: String) -> bool` — true solo si `id` es la mesa
    actualmente activa (siempre false mientras `is_in_lobby()` es true).
  - Task 2 consume esta clase tal cual, `id` será el nombre exacto del nodo
    de mesa (ej. `"BlackjackTableNet"`).

- [ ] **Step 1: Escribe los tests (deben fallar — la clase no existe aún)**

Crea `tests/unit/test_lobby_controller.gd`:

```gdscript
extends GutTest

func test_starts_in_lobby():
    var lobby = LobbyController.new()
    assert_true(lobby.is_in_lobby())

func test_no_table_is_active_while_in_lobby():
    var lobby = LobbyController.new()
    assert_false(lobby.is_active("BlackjackTableNet"))
    assert_false(lobby.is_active("DiceTableNet"))

func test_select_leaves_lobby_and_activates_table():
    var lobby = LobbyController.new()
    lobby.select("BlackjackTableNet")
    assert_false(lobby.is_in_lobby())
    assert_true(lobby.is_active("BlackjackTableNet"))

func test_only_selected_table_is_active():
    var lobby = LobbyController.new()
    lobby.select("DiceTableNet")
    assert_true(lobby.is_active("DiceTableNet"))
    assert_false(lobby.is_active("BlackjackTableNet"))
    assert_false(lobby.is_active("CrashTableNet"))

func test_selecting_another_table_switches_active_table():
    var lobby = LobbyController.new()
    lobby.select("BlackjackTableNet")
    lobby.select("PlinkoTableNet")
    assert_true(lobby.is_active("PlinkoTableNet"))
    assert_false(lobby.is_active("BlackjackTableNet"))

func test_return_to_lobby_resets_state():
    var lobby = LobbyController.new()
    lobby.select("PokerTableNet")
    lobby.return_to_lobby()
    assert_true(lobby.is_in_lobby())
    assert_false(lobby.is_active("PokerTableNet"))
```

- [ ] **Step 2: Corre los tests, confirma que fallan**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`
Expected: FAIL — `LobbyController` no existe (`Identifier not found`).

- [ ] **Step 3: Implementa `LobbyController`**

Crea `scripts/lobby/lobby_controller.gd`:

```gdscript
class_name LobbyController
extends RefCounted

var _active_id: String = ""

func is_in_lobby() -> bool:
    return _active_id == ""

func select(id: String) -> void:
    _active_id = id

func return_to_lobby() -> void:
    _active_id = ""

func is_active(id: String) -> bool:
    return not is_in_lobby() and _active_id == id
```

- [ ] **Step 4: Corre los tests, confirma que pasan**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`
Expected: PASS — los 6 tests nuevos en verde, y el resto de la suite
existente (blackjack/roulette/poker/dice/crash/mines/plinko/battle/goal)
sigue en verde.

- [ ] **Step 5: Commit**

```bash
git add scripts/lobby/lobby_controller.gd tests/unit/test_lobby_controller.gd
git commit -m "feat(lobby): add LobbyController pure navigation state"
```

---

## Task 2: Rejilla de lobby + salas aisladas + HUD persistente en `CasinoFloor`

**Files:**
- Modify: `scenes/casino_floor.tscn`
- Modify: `scripts/net/casino_floor.gd`
- Modify: `project.godot` (`window/size/viewport_height`)

**Interfaces:**
- Consumes: `LobbyController` de Task 1 (`is_in_lobby()`, `select(id)`,
  `return_to_lobby()`, `is_active(id)`).
- Produces: nada nuevo consumido por otras tareas — es la última pieza del
  plan.

### Contexto necesario antes de tocar `casino_floor.tscn`

Los 7 `*TableNet` que se instancian ya son `Control` con raíz
`anchors_preset = 15` (rectángulo completo) en su propia escena — **excepto
`poker_table_net.tscn`**, cuya raíz usa `anchors_preset = 0` con offsets
fijos (`offset_right = 1400.0`, `offset_bottom = 520.0`). Para que las 7
mesas ocupen pantalla completa cuando están activas, a Blackjack/Roulette/
Dice/Crash/Mines/Plinko les basta con **no overridear anchors** en su nodo
instanciado dentro de `casino_floor.tscn` (heredan el rect completo de su
escena base). Poker necesita override explícito de anchors en su instancia
dentro de `casino_floor.tscn` (no toques `poker_table_net.tscn` en sí, es
la escena base compartida y podría usarse aislada en otro contexto).

Los 7 nodos deben arrancar con `visible = false` — el `Lobby` es lo primero
que se ve.

### Estilo de las tarjetas

Placeholder "casino online": fondo verde fieltro oscuro, borde dorado,
texto dorado/crema, esquinas redondeadas. Dos `StyleBoxFlat` compartidos
(normal/hover) reutilizados por las 7 tarjetas.

- [ ] **Step 1: Reescribe `scenes/casino_floor.tscn` completo**

```tscn
[gd_scene load_steps=12 format=3]

[ext_resource type="PackedScene" path="res://scenes/blackjack_table_net.tscn" id="1"]
[ext_resource type="Script" path="res://scripts/net/casino_floor.gd" id="2"]
[ext_resource type="PackedScene" path="res://scenes/roulette_table_net.tscn" id="3"]
[ext_resource type="PackedScene" path="res://scenes/poker_table_net.tscn" id="4"]
[ext_resource type="Script" path="res://scripts/net/battle_controller.gd" id="5"]
[ext_resource type="PackedScene" path="res://scenes/dice_table_net.tscn" id="6"]
[ext_resource type="PackedScene" path="res://scenes/crash_table_net.tscn" id="7"]
[ext_resource type="PackedScene" path="res://scenes/mines_table_net.tscn" id="8"]
[ext_resource type="PackedScene" path="res://scenes/plinko_table_net.tscn" id="9"]

[sub_resource type="StyleBoxFlat" id="CardNormal"]
bg_color = Color(0.043, 0.184, 0.098, 1)
border_width_left = 3
border_width_top = 3
border_width_right = 3
border_width_bottom = 3
border_color = Color(0.816, 0.663, 0.176, 1)
corner_radius_top_left = 10
corner_radius_top_right = 10
corner_radius_bottom_right = 10
corner_radius_bottom_left = 10

[sub_resource type="StyleBoxFlat" id="CardHover"]
bg_color = Color(0.098, 0.298, 0.153, 1)
border_width_left = 3
border_width_top = 3
border_width_right = 3
border_width_bottom = 3
border_color = Color(0.816, 0.663, 0.176, 1)
corner_radius_top_left = 10
corner_radius_top_right = 10
corner_radius_bottom_right = 10
corner_radius_bottom_left = 10

[node name="CasinoFloor" type="Node2D"]
script = ExtResource("2")

[node name="BattleController" type="Node" parent="."]
script = ExtResource("5")

[node name="BlackjackTableNet" parent="." instance=ExtResource("1")]
visible = false

[node name="RouletteTableNet" parent="." instance=ExtResource("3")]
visible = false

[node name="PokerTableNet" parent="." instance=ExtResource("4")]
visible = false
anchor_right = 1.0
anchor_bottom = 1.0
offset_left = 0.0
offset_top = 0.0
offset_right = 0.0
offset_bottom = 0.0
grow_horizontal = 2
grow_vertical = 2

[node name="DiceTableNet" parent="." instance=ExtResource("6")]
visible = false

[node name="CrashTableNet" parent="." instance=ExtResource("7")]
visible = false

[node name="MinesTableNet" parent="." instance=ExtResource("8")]
visible = false

[node name="PlinkoTableNet" parent="." instance=ExtResource("9")]
visible = false

[node name="Lobby" type="Control" parent="."]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2

[node name="Title" type="Label" parent="Lobby"]
layout_mode = 0
offset_left = 20.0
offset_top = 20.0
offset_right = 880.0
offset_bottom = 60.0
theme_override_font_sizes/font_size = 32
theme_override_colors/font_color = Color(0.945, 0.847, 0.541, 1)
horizontal_alignment = 1
text = "Casino — elige tu mesa"

[node name="CardGrid" type="GridContainer" parent="Lobby"]
layout_mode = 0
offset_left = 60.0
offset_top = 130.0
offset_right = 840.0
offset_bottom = 900.0
theme_override_constants/h_separation = 20
theme_override_constants/v_separation = 20
columns = 2

[node name="BlackjackCard" type="Button" parent="Lobby/CardGrid"]
custom_minimum_size = Vector2(380, 160)
layout_mode = 2
theme_override_font_sizes/font_size = 24
theme_override_colors/font_color = Color(0.945, 0.847, 0.541, 1)
theme_override_styles/normal = SubResource("CardNormal")
theme_override_styles/hover = SubResource("CardHover")
theme_override_styles/pressed = SubResource("CardHover")
text = "Blackjack"

[node name="RouletteCard" type="Button" parent="Lobby/CardGrid"]
custom_minimum_size = Vector2(380, 160)
layout_mode = 2
theme_override_font_sizes/font_size = 24
theme_override_colors/font_color = Color(0.945, 0.847, 0.541, 1)
theme_override_styles/normal = SubResource("CardNormal")
theme_override_styles/hover = SubResource("CardHover")
theme_override_styles/pressed = SubResource("CardHover")
text = "Ruleta"

[node name="PokerCard" type="Button" parent="Lobby/CardGrid"]
custom_minimum_size = Vector2(380, 160)
layout_mode = 2
theme_override_font_sizes/font_size = 24
theme_override_colors/font_color = Color(0.945, 0.847, 0.541, 1)
theme_override_styles/normal = SubResource("CardNormal")
theme_override_styles/hover = SubResource("CardHover")
theme_override_styles/pressed = SubResource("CardHover")
text = "Póker"

[node name="DiceCard" type="Button" parent="Lobby/CardGrid"]
custom_minimum_size = Vector2(380, 160)
layout_mode = 2
theme_override_font_sizes/font_size = 24
theme_override_colors/font_color = Color(0.945, 0.847, 0.541, 1)
theme_override_styles/normal = SubResource("CardNormal")
theme_override_styles/hover = SubResource("CardHover")
theme_override_styles/pressed = SubResource("CardHover")
text = "Dice"

[node name="CrashCard" type="Button" parent="Lobby/CardGrid"]
custom_minimum_size = Vector2(380, 160)
layout_mode = 2
theme_override_font_sizes/font_size = 24
theme_override_colors/font_color = Color(0.945, 0.847, 0.541, 1)
theme_override_styles/normal = SubResource("CardNormal")
theme_override_styles/hover = SubResource("CardHover")
theme_override_styles/pressed = SubResource("CardHover")
text = "Crash"

[node name="MinesCard" type="Button" parent="Lobby/CardGrid"]
custom_minimum_size = Vector2(380, 160)
layout_mode = 2
theme_override_font_sizes/font_size = 24
theme_override_colors/font_color = Color(0.945, 0.847, 0.541, 1)
theme_override_styles/normal = SubResource("CardNormal")
theme_override_styles/hover = SubResource("CardHover")
theme_override_styles/pressed = SubResource("CardHover")
text = "Mines"

[node name="PlinkoCard" type="Button" parent="Lobby/CardGrid"]
custom_minimum_size = Vector2(380, 160)
layout_mode = 2
theme_override_font_sizes/font_size = 24
theme_override_colors/font_color = Color(0.945, 0.847, 0.541, 1)
theme_override_styles/normal = SubResource("CardNormal")
theme_override_styles/hover = SubResource("CardHover")
theme_override_styles/pressed = SubResource("CardHover")
text = "Plinko"

[node name="Hud" type="CanvasLayer" parent="."]

[node name="GoalLabel" type="Label" parent="Hud"]
offset_left = 20.0
offset_top = 10.0
offset_right = 700.0
offset_bottom = 34.0
text = "Meta colectiva: 0 / 1000 fichas"

[node name="UnlockedBanner" type="Label" parent="Hud"]
offset_left = 20.0
offset_top = 38.0
offset_right = 700.0
offset_bottom = 62.0
text = "¡Meta colectiva alcanzada! Nueva mesa desbloqueada."
visible = false

[node name="BattleStatusLabel" type="Label" parent="Hud"]
offset_left = 20.0
offset_top = 10.0
offset_right = 700.0
offset_bottom = 80.0
text = "Equipos: —"

[node name="BackButton" type="Button" parent="Hud"]
offset_left = 20.0
offset_top = 90.0
offset_right = 260.0
offset_bottom = 130.0
text = "‹ Volver al lobby"
visible = false
```

Nota: `CasinoFloor` sigue siendo `Node2D`; `Hud` es `CanvasLayer` (no
`Control`), así que sus hijos (`GoalLabel`/`UnlockedBanner`/
`BattleStatusLabel`/`BackButton`) se posicionan con `offset_left/top/right/
bottom` en coordenadas de pantalla absolutas — mismo patrón que ya usaban
como hijos directos de `CasinoFloor` antes de este cambio, solo que ahora
un `CanvasLayer` los dibuja siempre por encima del lobby y de cualquier
mesa (capa por defecto de `CanvasLayer` ya está por encima de los
`CanvasItem` en el árbol principal, no hace falta fijar `layer` a mano).

- [ ] **Step 2: Baja `viewport_height` en `project.godot`**

Modifica `project.godot` — busca la línea:

```
window/size/viewport_height=2650
```

Cámbiala a:

```
window/size/viewport_height=1080
```

(`window/size/viewport_width=900` no cambia — las 7 mesas ya usan anchors
que escalan con el viewport, no dependen de un ancho concreto.)

- [ ] **Step 3: Reescribe `scripts/net/casino_floor.gd`**

```gdscript
extends Node2D

const GOAL_TARGET := 1000
const BATTLE_GOAL_BALANCE := 2000
const BATTLE_TIME_LIMIT_SEC := 600.0
const BATTLE_STARTING_BALANCE := 500

const TABLE_NODE_NAMES := [
    "BlackjackTableNet",
    "RouletteTableNet",
    "PokerTableNet",
    "DiceTableNet",
    "CrashTableNet",
    "MinesTableNet",
    "PlinkoTableNet",
]

@onready var goal_label: Label = $Hud/GoalLabel
@onready var unlocked_banner: Label = $Hud/UnlockedBanner
@onready var battle_controller: BattleController = $BattleController
@onready var battle_status_label: Label = $Hud/BattleStatusLabel
@onready var lobby_view: Control = $Lobby
@onready var card_grid: GridContainer = $Lobby/CardGrid
@onready var back_button: Button = $Hud/BackButton

var goal: CollectiveGoal
var _is_battle_mode: bool
var _teams_line: String = ""
var _state_line: String = ""

var _lobby := LobbyController.new()
var _table_nodes: Dictionary = {}

func _ready() -> void:
    for table_name in TABLE_NODE_NAMES:
        _table_nodes[table_name] = get_node(table_name)
    for card in card_grid.get_children():
        if card is BaseButton:
            card.pressed.connect(_on_card_pressed.bind(card.name))
    back_button.pressed.connect(_on_back_pressed)
    _refresh_room_visibility()

    _is_battle_mode = SteamManager.chosen_match_type != -1
    goal_label.visible = not _is_battle_mode
    unlocked_banner.visible = false
    battle_status_label.visible = _is_battle_mode
    if _is_battle_mode:
        battle_controller.teams_changed.connect(_on_teams_changed)
        battle_controller.match_state_changed.connect(_on_match_state_changed)
    if multiplayer.is_server():
        if _is_battle_mode:
            battle_controller.start_match(SteamManager.chosen_match_type, BATTLE_GOAL_BALANCE, BATTLE_TIME_LIMIT_SEC, BATTLE_STARTING_BALANCE)
            battle_controller.join(multiplayer.get_unique_id())
        else:
            goal = CollectiveGoal.new(GOAL_TARGET)
            for controller in find_children("*", "TableController", true, false):
                controller.chips_won.connect(_on_chips_won)
            for controller in find_children("*", "DiceTableController", true, false):
                controller.chips_won.connect(_on_chips_won)
            for controller in find_children("*", "CrashTableController", true, false):
                controller.chips_won.connect(_on_chips_won)
            for controller in find_children("*", "MinesTableController", true, false):
                controller.chips_won.connect(_on_chips_won)
            for controller in find_children("*", "PlinkoTableController", true, false):
                controller.chips_won.connect(_on_chips_won)
            _broadcast_goal_state()
    else:
        var peer := multiplayer.multiplayer_peer
        if peer == null or peer.get_connection_status() != MultiplayerPeer.CONNECTION_CONNECTED:
            multiplayer.connected_to_server.connect(_on_connected_to_server)
        else:
            _on_connected_to_server()

func _on_connected_to_server() -> void:
    if _is_battle_mode:
        battle_controller.join(multiplayer.get_unique_id())
        battle_controller.request_state()
    else:
        request_goal_state()

# ---- lobby: navegación local entre sala y mesas (sin red) ----

func _on_card_pressed(card_name: String) -> void:
    var table_name: String = card_name.replace("Card", "TableNet")
    if not _table_nodes.has(table_name):
        return
    _lobby.select(table_name)
    _refresh_room_visibility()

func _on_back_pressed() -> void:
    _lobby.return_to_lobby()
    _refresh_room_visibility()

func _refresh_room_visibility() -> void:
    lobby_view.visible = _lobby.is_in_lobby()
    back_button.visible = not _lobby.is_in_lobby()
    for table_name in _table_nodes:
        _table_nodes[table_name].visible = _lobby.is_active(table_name)

# ---- modo libre: meta colectiva ----

func _on_chips_won(_player_id: int, amount: int) -> void:
    goal.add_chips(amount)
    _broadcast_goal_state()

# Un cliente que entra a CasinoFloor después de que ya hubo ganancias no
# recibe nada por su cuenta: el host solo retransmite _receive_goal_state
# cuando el contador cambia, nunca al conectar (mismo gotcha que
# TableController.request_state en Plan 3). Sin este pedido explícito el
# cliente se queda con el contador en 0 para siempre.
func request_goal_state() -> void:
    if multiplayer.is_server():
        return
    _request_goal_state.rpc_id(1)

@rpc("any_peer", "call_remote", "reliable")
func _request_goal_state() -> void:
    if not multiplayer.is_server():
        return
    _receive_goal_state.rpc_id(multiplayer.get_remote_sender_id(), goal.to_dict())

func _broadcast_goal_state() -> void:
    _receive_goal_state.rpc(goal.to_dict())

@rpc("authority", "call_local", "reliable")
func _receive_goal_state(state: Dictionary) -> void:
    goal_label.text = "Meta colectiva: %d / %d fichas" % [state["total"], state["target"]]
    unlocked_banner.visible = state["unlocked"]

# ---- modo batalla ----

func _on_teams_changed(teams: Array) -> void:
    _teams_line = "Equipo A: %s | Equipo B: %s" % [str(teams[0]), str(teams[1])]
    _refresh_battle_label()

func _on_match_state_changed(state: Dictionary) -> void:
    var msg := "Pozo A: %d | Pozo B: %d" % [state["pool_balances"][0], state["pool_balances"][1]]
    if state["finished"]:
        msg += " — FIN (equipo %d, %s)" % [state["winning_team"], state["reason"]]
    _state_line = msg
    _refresh_battle_label()

func _refresh_battle_label() -> void:
    battle_status_label.text = _teams_line + "\n" + _state_line
```

El único cambio de comportamiento real frente al archivo original es el
bloque `_ready` (paths de `@onready` apuntando a `$Hud/...` y `$Lobby`,
más el wiring de tarjetas/back-button al principio de `_ready`) y las tres
funciones nuevas `_on_card_pressed`/`_on_back_pressed`/
`_refresh_room_visibility`. El resto (meta colectiva, batalla, RPCs) es
carácter por carácter el mismo código que ya existía.

- [ ] **Step 4: Corre la suite completa de tests (regresión)**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`
Expected: PASS — toda la suite (incluida `test_lobby_controller.gd` de
Task 1) en verde. Esto no ejercita `casino_floor.tscn` directamente, pero
al arrancar el proyecto headless Godot parsea todos los scripts/autoloads;
un error de sintaxis en `casino_floor.gd` o una referencia rota
(`$Hud/GoalLabel` mal escrito, etc.) hace fallar el arranque del runner.

- [ ] **Step 5: Verificación manual (abre el proyecto en el editor de Godot)**

Con el editor de Godot abierto en el proyecto, ejecuta la escena principal
(la que carga `CasinoFloor`, o `casino_floor.tscn` directamente con F6 si
tiene autoloads suficientes para arrancar en modo standalone/host) y
comprueba a mano:

1. Al entrar se ve el lobby: título + 7 tarjetas (Blackjack, Ruleta, Póker,
   Dice, Crash, Mines, Plinko), ninguna mesa visible.
2. El HUD (meta colectiva o marcador de batalla según el modo) es visible
   sobre el lobby.
3. Pulsar una tarjeta oculta el lobby y muestra esa mesa a pantalla
   completa, con el botón "‹ Volver al lobby" visible; el HUD sigue
   visible sobre la mesa.
4. Jugar una ronda en esa mesa funciona igual que antes (verifica al menos
   una: Blackjack o Dice) — confirma que ocultar/mostrar nodos no rompió
   ninguna interacción de red.
5. Pulsar "Volver al lobby" oculta la mesa y vuelve a mostrar la rejilla de
   7 tarjetas; el HUD no parpadeó ni se ocultó en ningún momento de la
   transición.
6. Repite con una segunda instancia del juego en local (dos ventanas,
   host+cliente) y confirma que cada una navega su propia sala sin afectar
   a la otra (una en Blackjack, la otra en Dice, por ejemplo) — la meta
   colectiva sigue sumando fichas de ambas mesas en el HUD de las dos
   instancias.

- [ ] **Step 6: Commit**

```bash
git add scenes/casino_floor.tscn scripts/net/casino_floor.gd project.godot
git commit -m "feat(lobby): add game selection grid with isolated per-client rooms"
```

---

## Self-Review

**Cobertura de la spec (sección "Ampliación v1.2"):**
- Rejilla de 7 tarjetas con icono/nombre pulsable → Task 2 (`Lobby/CardGrid`
  con 7 `Button` estilizados).
- Sala aislada, botón volver → Task 2 (`_on_card_pressed`,
  `_on_back_pressed`, `BackButton`).
- Independencia entre jugadores, sin sincronización de red → Task 1+2:
  `LobbyController` vive solo en memoria local de cada cliente, nunca se
  serializa ni se manda por RPC.
- HUD persistente en lobby y sala → Task 2 (`Hud` como `CanvasLayer`
  separado, nunca se oculta al cambiar de sala).
- Estética "casino online" → Task 2 (`StyleBoxFlat` verde fieltro/borde
  dorado en las tarjetas).
- Reestructuración de `casino_floor.tscn` (mesas ocultas por defecto,
  layout full-rect, `viewport_height` de vuelta a tamaño normal) → Task 2,
  Steps 1-2 (`1080`).
- No tocar `GameLogic`/`TableState`/`TableController` → verificado, ningún
  archivo de esas carpetas aparece en los diffs de este plan.

Sin huecos pendientes.
