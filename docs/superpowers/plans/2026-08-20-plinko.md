# Plinko (Plan 11) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Añadir el módulo Plinko (bola cae por `rows` filas de clavijas, slot final = número de rebotes a la derecha, tabla de multiplicadores binomial invertida normalizada a 99% de retorno esperado) como mesa multijugador más en `CasinoFloor`, reutilizando el patrón de ronda independiente por jugador que fijó Plan 8 (Dice).

**Architecture:** `PlinkoRoller` (lógica pura, RNG inyectable en tests, análoga a `DiceRoller`) genera los `rows` bits izquierda/derecha del host. `PlinkoTableState` (lógica pura, vive solo en el host, análoga a `DiceTableState`: `Dictionary` `players` indexado por `player_id`, cada jugador se crea perezosamente en su primera tirada) valida `rows`/apuesta, cuenta los rebotes a la derecha para obtener el slot, calcula el multiplicador de ese slot con una fórmula binomial cerrada (`c / C(rows, slot)` con `c` elegido para que el retorno esperado total dé exactamente 0.99), paga contra `ChipLedger` y emite `chips_won` solo cuando el pago neto es positivo. `PlinkoTableController` (nodo de red, mismo patrón host-autoridad que `DiceTableController`) recibe la tirada del cliente por RPC, la aplica sobre `PlinkoTableState` y retransmite el estado resultante con broadcast a todos los presentes en `CasinoFloor`. La escena `PlinkoTableNet` aloja la mesa y se añade a `CasinoFloor` en su propia franja vertical, debajo de Dice.

**Tech Stack:** Godot 4.4+, GDScript, GUT (lógica de `PlinkoRoller`/`PlinkoTableState`, sin red), `MultiplayerAPI`/RPCs nativos de Godot sobre el `SteamMultiplayerPeer` ya operativo (Plan 2/3).

**Spec:** `docs/superpowers/specs/2026-08-17-casino-multiplayer-design.md` (sección "Ampliación v1.1", subsecciones "Componente nuevo: patrón de ronda independiente" y "Plinko"). Interfaz base a reutilizar: `docs/superpowers/plans/2026-08-19-dice.md`, sección final "Patrón de ronda independiente para Crash/Mines/Plinko".

## Global Constraints

- No se toca `scripts/net/table_controller.gd`, `scripts/net/roulette_table_controller.gd`, `scripts/net/poker_table_controller.gd`, `scripts/net/battle_controller.gd`, `scripts/net/dice_table_controller.gd` ni sus escenas — Plinko añade sus propias clases y escenas paralelas.
- Servidor autoritativo: toda decisión de juego (bits de rebote, validación, pago) ocurre solo en el host (`multiplayer.is_server()`); los clientes solo envían intención y reciben estado.
- El servidor tiene siempre `unique_id == 1` — los clientes dirigen sus RPCs de acción con `rpc_id(1, ...)`.
- Gotcha de `rpc_id(1, ...)` sobre uno mismo (ya resuelto en Plan 3/8): `PlinkoTableController` expone un método wrapper (`roll`) que en el host aplica la acción directamente al estado en vez de auto-llamarse por RPC; los clientes sí pasan por la RPC `request_roll`.
- Gotcha de layout de Ruleta (commit `cb50b8e`): los botones usan offsets absolutos en píxeles, no encogen con el contenedor — dimensionar cada botón al ancho real de su texto en español, no reutilizar anchos genéricos.
- **Sin asientos.** Igual que Dice: no hay `SEAT_COUNT` ni `sit()`. El estado por jugador se crea perezosamente la primera vez que ese `player_id` tira.
- Margen de casa: 1% (retorno esperado total 0.99), igual que los otros tres juegos, incorporado en la fórmula del multiplicador por slot, no como impuesto aparte.
- Balance inicial por jugador: 500 fichas (`ChipLedger.new(500)`).
- Sin persistencia entre partidas — el estado de la mesa vive solo en memoria del host mientras dura la sesión.
- Rango válido de `rows`: 8-16 inclusive, por defecto 12 (la spec no fija el rango exacto, solo el default; se acota aquí para que `C(rows, rows/2)` y `2^rows` se mantengan en valores pequeños y sin problemas de precisión, evitando además tableros irrisoriamente cortos o largos).
- No hace falta simular física real de la bola — solo el resultado final (slot, derivado de contar bits a la derecha) importa para el pago.

---

## Task 1: PlinkoRoller — bits de rebote con resultado inyectable para tests

**Files:**
- Create: `scripts/plinko/plinko_roller.gd`
- Test: `tests/unit/test_plinko_roller.gd`

**Interfaces:**
- Produce: `PlinkoRoller.new()`, propiedad `results: Array` (cola de secuencias de rebotes forzadas, cada elemento un `Array[bool]` de longitud `rows`, vacía por defecto), método `roll(rows: int) -> Array` — si `results` no está vacío, hace `pop_front()` y devuelve esa secuencia tal cual (se asume que quien la puso en la cola ya la dimensionó a `rows`); si está vacío, genera y devuelve un `Array[bool]` de longitud `rows` donde cada elemento es `randf() < 0.5` (`true` = rebote a la derecha). Usado por `PlinkoTableState` (Task 2/3).

- [ ] **Step 1: Escribir los tests**

```gdscript
extends GutTest

func test_roll_returns_queued_bounces_in_order():
	var roller = PlinkoRoller.new()
	roller.results = [[true, false, true], [false, false, false]]
	assert_eq(roller.roll(3), [true, false, true])
	assert_eq(roller.roll(3), [false, false, false])

func test_roll_without_queue_returns_rows_bools():
	var roller = PlinkoRoller.new()
	var bounces = roller.roll(12)
	assert_eq(bounces.size(), 12)
	for bounced_right in bounces:
		assert_true(bounced_right is bool)
```

Guardar en `tests/unit/test_plinko_roller.gd`.

- [ ] **Step 2: Ejecutar y verificar que fallan**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`
Expected: FAIL — `Identifier "PlinkoRoller" not declared`.

- [ ] **Step 3: Implementar PlinkoRoller**

```gdscript
class_name PlinkoRoller
extends RefCounted

var results: Array = []

func roll(rows: int) -> Array:
	if not results.is_empty():
		return results.pop_front()
	var bounces: Array[bool] = []
	for i in range(rows):
		bounces.append(randf() < 0.5)
	return bounces
```

Guardar en `scripts/plinko/plinko_roller.gd`.

- [ ] **Step 4: Ejecutar y verificar que pasan**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`
Expected: todos los tests de `test_plinko_roller.gd` en verde, `0 failed`.

- [ ] **Step 5: Commit**

```bash
git add scripts/plinko/plinko_roller.gd tests/unit/test_plinko_roller.gd
git commit -m "feat: add PlinkoRoller with test-injectable bounce queue"
```

---

## Task 2: PlinkoTableState — jugadores perezosos y fórmula de multiplicador por slot

**Files:**
- Create: `scripts/plinko/plinko_table_state.gd`
- Test: `tests/unit/test_plinko_table_state.gd`

**Interfaces:**
- Consume: `ChipLedger` (Plan 1, `scripts/chip_ledger.gd`), `PlinkoRoller` (Task 1, `scripts/plinko/plinko_roller.gd`).
- Produce: `PlinkoTableState.new(roller: PlinkoRoller = null)`, constantes `STARTING_BALANCE = 500`, `MIN_ROWS = 8`, `MAX_ROWS = 16`, `DEFAULT_ROWS = 12`, propiedad `players: Dictionary` (`player_id: int -> Player`, vacío al empezar; `Player` tiene `player_id: int`, `ledger: ChipLedger`, `last_round: Dictionary` — `{}` hasta la primera tirada), métodos estáticos `_comb(n: int, k: int) -> int` (combinatorio, `C(n, k)`) y `slot_multiplier(rows: int, slot: int) -> float` (multiplicador del slot, retorno esperado total exactamente 0.99). Usado por Task 3 (mismo archivo, añade `roll`/`to_dict`) y por `PlinkoTableController` (Task 4).

**Nota de diseño (fórmula del multiplicador):** con `rows` filas, la probabilidad de caer en el slot `k` (0..rows) es `C(rows, k) / 2^rows` (binomial justa, 50/50 por rebote). Se define `mult(rows, k) = c / C(rows, k)` con `c = 0.99 * 2^rows / (rows + 1)`. El retorno esperado total es entonces `sum_k [C(rows,k)/2^rows] * [c/C(rows,k)] = (rows+1) * c / 2^rows = 0.99` **exactamente**, para cualquier `rows` — no hace falta simular para verificarlo, la fórmula lo garantiza matemáticamente y el test lo confirma sumando `probabilidad * multiplicador` de todos los slots. Los extremos (`k=0` o `k=rows`, `C(rows,k)=1`) pagan el máximo (~312x con `rows=12`); el centro (`k=rows/2`, `C(rows,k)` máximo) paga menos de 1x — coincide con la curva "binomial invertida" de la spec.

- [ ] **Step 1: Escribir los tests**

```gdscript
extends GutTest

func test_comb_known_values():
	assert_eq(PlinkoTableState._comb(12, 0), 1)
	assert_eq(PlinkoTableState._comb(12, 6), 924)
	assert_eq(PlinkoTableState._comb(12, 12), 1)

func test_slot_multiplier_symmetric_and_edges_pay_more():
	var mult_edge = PlinkoTableState.slot_multiplier(12, 0)
	var mult_center = PlinkoTableState.slot_multiplier(12, 6)
	assert_true(mult_edge > mult_center)
	assert_almost_eq(mult_edge, PlinkoTableState.slot_multiplier(12, 12), 0.0001)
	assert_true(mult_center < 1.0)

func test_slot_multiplier_expected_return_is_99_percent():
	for rows in [8, 12, 16]:
		var expected_return := 0.0
		var total_outcomes: float = pow(2.0, rows)
		for slot in range(rows + 1):
			var probability: float = PlinkoTableState._comb(rows, slot) / total_outcomes
			expected_return += probability * PlinkoTableState.slot_multiplier(rows, slot)
		assert_almost_eq(expected_return, 0.99, 0.0001)

func test_players_starts_empty():
	var table = PlinkoTableState.new()
	assert_eq(table.players.size(), 0)
```

Guardar en `tests/unit/test_plinko_table_state.gd`.

- [ ] **Step 2: Ejecutar y verificar que fallan**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`
Expected: FAIL — `Identifier "PlinkoTableState" not declared`.

- [ ] **Step 3: Implementar PlinkoTableState (parte 1: jugadores y fórmula)**

```gdscript
class_name PlinkoTableState
extends RefCounted

signal chips_won(player_id: int, amount: int)

const STARTING_BALANCE := 500
const MIN_ROWS := 8
const MAX_ROWS := 16
const DEFAULT_ROWS := 12

class Player:
	var player_id: int = 0
	var ledger: ChipLedger
	var last_round: Dictionary = {}

var players: Dictionary = {}
var roller: PlinkoRoller

func _init(p_roller: PlinkoRoller = null) -> void:
	roller = p_roller if p_roller else PlinkoRoller.new()

static func _comb(n: int, k: int) -> int:
	if k < 0 or k > n:
		return 0
	k = min(k, n - k)
	var result := 1
	for i in range(k):
		result = result * (n - i) / (i + 1)
	return result

static func slot_multiplier(rows: int, slot: int) -> float:
	var c: float = 0.99 * pow(2.0, rows) / (rows + 1)
	return c / _comb(rows, slot)

func _player_for(player_id: int) -> Player:
	if not players.has(player_id):
		var player := Player.new()
		player.player_id = player_id
		player.ledger = ChipLedger.new(STARTING_BALANCE)
		players[player_id] = player
	return players[player_id]
```

Guardar en `scripts/plinko/plinko_table_state.gd`. `roll`/`to_dict` se añaden en Task 3 — con esto ya deberían pasar los tests de Task 2.

- [ ] **Step 4: Ejecutar y verificar que pasan**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`
Expected: todos los tests de `test_plinko_table_state.gd` en verde, `0 failed`.

- [ ] **Step 5: Commit**

```bash
git add scripts/plinko/plinko_table_state.gd tests/unit/test_plinko_table_state.gd
git commit -m "feat: add PlinkoTableState with lazy players and binomial payout formula"
```

---

## Task 3: PlinkoTableState — tirada, resolución de pago y to_dict

**Files:**
- Modify: `scripts/plinko/plinko_table_state.gd`
- Modify: `tests/unit/test_plinko_table_state.gd`

**Interfaces:**
- Produce (añadido a `PlinkoTableState`): `roll(player_id: int, rows: int, amount: int) -> bool` (valida `rows` en `[MIN_ROWS, MAX_ROWS]` y apuesta contra el ledger del jugador — creándolo perezosamente si es su primera tirada —, tira con `roller.roll(rows)`, cuenta rebotes a la derecha para obtener el slot, calcula `slot_multiplier(rows, slot)`, paga siempre `int(amount * multiplicador)` al ledger (a diferencia de Dice, en Plinko no hay pérdida total: incluso los slots centrales devuelven una fracción de la apuesta), emite `chips_won(player_id, net_profit)` solo cuando el pago neto es positivo, y guarda el resultado en `players[player_id].last_round`), `to_dict() -> Dictionary` (`{"players": {player_id: {"player_id", "balance", "last_round"}}}`).

- [ ] **Step 1: Añadir los tests de tirada y resolución**

Añadir al final de `tests/unit/test_plinko_table_state.gd`:

```gdscript
func test_roll_fails_on_rows_out_of_range():
	var table = PlinkoTableState.new()
	assert_false(table.roll(111, 7, 50))
	assert_false(table.roll(111, 17, 50))

func test_roll_fails_on_insufficient_balance():
	var table = PlinkoTableState.new()
	var ok = table.roll(111, 12, 5000)
	assert_false(ok)
	# _player_for crea la entrada perezosa (balance 500) antes de intentar el
	# place_bet de 5000; el jugador queda creado pero intacto, no se descuenta.
	assert_eq(table.players[111].ledger.balance, 500)

func test_roll_wins_edge_slot_pays_large_multiplier():
	var roller = PlinkoRoller.new()
	var bounces: Array[bool] = [true, true, true, true, true, true, true, true, true, true, true, true]
	roller.results = [bounces]
	var table = PlinkoTableState.new(roller)
	var ok = table.roll(111, 12, 100)
	assert_true(ok)
	# slot 12, multiplicador 0.99*4096/13/1 = 311.9261... -> payout int(100*311.9261...) = 31192
	assert_eq(table.players[111].ledger.balance, 500 - 100 + 31192)
	assert_true(table.players[111].last_round["win"])

func test_roll_loses_center_slot_partial_payout():
	var roller = PlinkoRoller.new()
	var bounces: Array[bool] = [true, false, true, false, true, false, true, false, true, false, true, false]
	roller.results = [bounces]
	var table = PlinkoTableState.new(roller)
	var ok = table.roll(111, 12, 100)
	assert_true(ok)
	# slot 6, multiplicador 0.99*4096/13/924 = 0.337582... -> payout int(100*0.337582...) = 33
	assert_eq(table.players[111].ledger.balance, 500 - 100 + 33)
	assert_false(table.players[111].last_round["win"])

func test_roll_emits_chips_won_with_net_profit_on_win():
	var roller = PlinkoRoller.new()
	var bounces: Array[bool] = [true, true, true, true, true, true, true, true, true, true, true, true]
	roller.results = [bounces]
	var table = PlinkoTableState.new(roller)
	watch_signals(table)
	table.roll(111, 12, 100)
	assert_signal_emitted_with_parameters(table, "chips_won", [111, 31092]) # 31192 payout - 100 apuesta

func test_roll_does_not_emit_chips_won_on_loss():
	var roller = PlinkoRoller.new()
	var bounces: Array[bool] = [true, false, true, false, true, false, true, false, true, false, true, false]
	roller.results = [bounces]
	var table = PlinkoTableState.new(roller)
	watch_signals(table)
	table.roll(111, 12, 100)
	assert_signal_not_emitted(table, "chips_won")

func test_roll_records_last_round_for_player():
	var roller = PlinkoRoller.new()
	var bounces: Array[bool] = [true, true, true, true, true, true, true, true, true, true, true, true]
	roller.results = [bounces]
	var table = PlinkoTableState.new(roller)
	table.roll(111, 12, 100)
	var last_round = table.players[111].last_round
	assert_eq(last_round["rows"], 12)
	assert_eq(last_round["amount"], 100)
	assert_eq(last_round["bounces"], bounces)
	assert_eq(last_round["slot"], 12)
	assert_eq(last_round["payout"], 31192)
	assert_true(last_round["win"])

func test_roll_independent_per_player():
	var roller = PlinkoRoller.new()
	var edge_bounces: Array[bool] = [true, true, true, true, true, true, true, true, true, true, true, true]
	var center_bounces: Array[bool] = [true, false, true, false, true, false, true, false, true, false, true, false]
	roller.results = [edge_bounces, center_bounces]
	var table = PlinkoTableState.new(roller)
	table.roll(111, 12, 100) # gana grande
	table.roll(222, 12, 100) # pierde parcial
	assert_eq(table.players[111].ledger.balance, 31592)
	assert_eq(table.players[222].ledger.balance, 433)

func test_to_dict_reflects_players_and_last_round():
	var roller = PlinkoRoller.new()
	var bounces: Array[bool] = [true, true, true, true, true, true, true, true, true, true, true, true]
	roller.results = [bounces]
	var table = PlinkoTableState.new(roller)
	table.roll(111, 12, 100)
	var data = table.to_dict()
	assert_eq(data["players"][111]["player_id"], 111)
	assert_eq(data["players"][111]["balance"], 31592)
	assert_eq(data["players"][111]["last_round"]["win"], true)
```

- [ ] **Step 2: Ejecutar y verificar que fallan**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`
Expected: FAIL — `Invalid call. Nonexistent function 'roll'/'to_dict'`.

- [ ] **Step 3: Implementar roll/resolución/to_dict**

Añadir dentro de `scripts/plinko/plinko_table_state.gd`:

```gdscript
func roll(player_id: int, rows: int, amount: int) -> bool:
	if rows < MIN_ROWS or rows > MAX_ROWS:
		return false
	var player := _player_for(player_id)
	if not player.ledger.place_bet(amount):
		return false
	var bounces: Array = roller.roll(rows)
	var slot := 0
	for bounced_right in bounces:
		if bounced_right:
			slot += 1
	var mult := slot_multiplier(rows, slot)
	var payout := int(amount * mult)
	var win := payout > amount
	player.ledger.payout(payout)
	if win:
		chips_won.emit(player_id, payout - amount)
	player.last_round = {
		"rows": rows,
		"amount": amount,
		"bounces": bounces,
		"slot": slot,
		"multiplier": mult,
		"payout": payout,
		"win": win,
	}
	return true

func to_dict() -> Dictionary:
	var players_data := {}
	for player_id in players:
		var player = players[player_id]
		players_data[player_id] = {
			"player_id": player.player_id,
			"balance": player.ledger.balance,
			"last_round": player.last_round,
		}
	return {"players": players_data}
```

- [ ] **Step 4: Ejecutar y verificar que pasan**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`
Expected: todos los tests en verde, `0 failed`, incluidos los de Task 2.

- [ ] **Step 5: Commit**

```bash
git add scripts/plinko/plinko_table_state.gd tests/unit/test_plinko_table_state.gd
git commit -m "feat: add roll resolution and payouts to PlinkoTableState"
```

---

## Task 4: PlinkoTableController — puente de red sobre PlinkoTableState

**Files:**
- Create: `scripts/net/plinko_table_controller.gd`

**Interfaces:**
- Consume: `PlinkoTableState` (Task 2/3 completo), `multiplayer.is_server()`, `multiplayer.get_remote_sender_id()`, `multiplayer.get_unique_id()` (API estándar de Godot).
- Produce: nodo `PlinkoTableController` con señales `state_changed(state: Dictionary)` y `chips_won(player_id: int, amount: int)`, método de entrada `roll(rows: int, amount: int)`, y `request_state()` — aplica la acción directamente si es el host, o hace `rpc_id(1, ...)` si es cliente (mismo wrapper que `DiceTableController`). Usado por `PlinkoTableNet` (Task 5) y por `CasinoFloor` (hook de `chips_won` para la meta colectiva, Task 5).

- [ ] **Step 1: Implementar PlinkoTableController**

```gdscript
class_name PlinkoTableController
extends Node

signal state_changed(state: Dictionary)
signal chips_won(player_id: int, amount: int)

var table_state: PlinkoTableState

func _ready() -> void:
	if multiplayer.is_server():
		table_state = PlinkoTableState.new()
		table_state.chips_won.connect(_on_table_chips_won)

# Puntos de entrada para la UI local (host o cliente). El host actúa directo
# sobre table_state; un rpc_id(1, ...) a sí mismo lo rechaza el MultiplayerAPI
# ("call_remote" no permite invocarse a uno mismo), así que el host no puede
# pasar por las RPCs request_* para sus propias acciones.
func roll(rows: int, amount: int) -> void:
	if multiplayer.is_server():
		_apply_roll(rows, amount, multiplayer.get_unique_id())
	else:
		request_roll.rpc_id(1, rows, amount)

# Un cliente que entra a la mesa después de que ya hubo tiradas no recibe
# nada por su cuenta: el host solo retransmite _receive_state cuando algo
# cambia, nunca al conectar (mismo gotcha que TableController.request_state
# en Plan 3). Sin este pedido explícito el cliente se queda con el estado en
# blanco para siempre.
func request_state() -> void:
	if multiplayer.is_server():
		return
	_request_state.rpc_id(1)

@rpc("any_peer", "call_remote", "reliable")
func _request_state() -> void:
	if not multiplayer.is_server():
		return
	_receive_state.rpc_id(multiplayer.get_remote_sender_id(), table_state.to_dict())

@rpc("any_peer", "call_remote", "reliable")
func request_roll(rows: int, amount: int) -> void:
	if not multiplayer.is_server():
		return
	_apply_roll(rows, amount, multiplayer.get_remote_sender_id())

func _apply_roll(rows: int, amount: int, player_id: int) -> void:
	if table_state.roll(player_id, rows, amount):
		_broadcast_state()

func _broadcast_state() -> void:
	_receive_state.rpc(table_state.to_dict())

# Broadcast a TODOS los presentes en CasinoFloor, no solo a quien tiró — la
# spec pide visibilidad compartida de las rondas de los demás aunque nadie
# más participe en ellas.
@rpc("authority", "call_local", "reliable")
func _receive_state(state: Dictionary) -> void:
	state_changed.emit(state)

func _on_table_chips_won(player_id: int, amount: int) -> void:
	chips_won.emit(player_id, amount)
```

Guardar en `scripts/net/plinko_table_controller.gd`. Nota: en el host, `_ready()` crea `table_state` y conecta su `chips_won`; en los clientes queda `null` a propósito — los clientes nunca ejecutan lógica de juego, solo reciben `state_changed`/`chips_won` y piden acciones por RPC.

- [ ] **Step 2: Verificación manual con dos cuentas** (⚠️ requiere dos cuentas Steam en vivo, no se puede automatizar)

Reutilizando el flujo de lobby ya operativo (Plan 2/3), añade temporalmente un `PlinkoTableController` como nodo hijo de la escena principal en ambas instancias y conecta:

```gdscript
$PlinkoTableController.state_changed.connect(func(state): print("Estado plinko: %s" % state))
```

Desde la instancia cliente, llama `$PlinkoTableController.roll(12, 100)`. Confirma que **ambas** instancias imprimen el mismo `Estado plinko` tras la tirada (la del host porque es la fuente, la del cliente porque le llegó por `_receive_state`), que el balance del jugador que tiró cambia igual en las dos, y que el jugador que no tiró también ve el `players` dict actualizado con la tirada del otro — confirma la visibilidad compartida.

- [ ] **Step 3: Commit**

```bash
git add scripts/net/plinko_table_controller.gd
git commit -m "feat: add PlinkoTableController RPC bridge for PlinkoTableState"
```

---

## Task 5: Escena PlinkoTableNet + integración en CasinoFloor

**Files:**
- Create: `scenes/plinko_table_net.gd`
- Create: `scenes/plinko_table_net.tscn`
- Modify: `scenes/casino_floor.tscn`
- Modify: `scripts/net/casino_floor.gd`

**Interfaces:**
- Consume: `PlinkoTableController` (Task 4), `NetworkManager.peer_steam_ids`/`identities_changed` (Plan 2, ya usado por `dice_table_net.gd`).

- [ ] **Step 1: Escribir el script de la mesa jugable**

```gdscript
extends Control

@onready var table_controller: PlinkoTableController = $TableController
@onready var players_label: Label = $PlayersLabel
@onready var rows_spinbox: SpinBox = $RowsSpinBox
@onready var amount_spinbox: SpinBox = $AmountSpinBox
@onready var drop_button: Button = $DropButton

var _last_players: Dictionary = {}

func _display_name(peer_id: int) -> String:
	var steam_id: int = NetworkManager.peer_steam_ids.get(peer_id, 0)
	if steam_id == 0:
		return "jugador %d" % peer_id
	var persona_name := Steam.getFriendPersonaName(steam_id)
	return persona_name if not persona_name.is_empty() else "jugador %d" % peer_id

func _ready() -> void:
	table_controller.state_changed.connect(_on_state_changed)
	drop_button.pressed.connect(_drop)
	NetworkManager.identities_changed.connect(_refresh_players_label)
	# Mismo gotcha que dice_table_net.gd/roulette_table_net.gd: un cliente
	# recién llegado a esta escena puede que aún no tenga el
	# SteamMultiplayerPeer en CONNECTED — bloquear la apuesta hasta entonces
	# evita el rpc_id() fallido.
	if not multiplayer.is_server():
		var peer := multiplayer.multiplayer_peer
		if peer == null or peer.get_connection_status() != MultiplayerPeer.CONNECTION_CONNECTED:
			drop_button.disabled = true
			multiplayer.connected_to_server.connect(func():
				drop_button.disabled = false
				table_controller.request_state()
			)
		else:
			table_controller.request_state()

func _drop() -> void:
	table_controller.roll(int(rows_spinbox.value), int(amount_spinbox.value))

func _on_state_changed(state: Dictionary) -> void:
	_last_players = state["players"]
	_refresh_players_label()

func _refresh_players_label() -> void:
	if _last_players.is_empty():
		players_label.text = "Nadie ha soltado la bola todavía."
		return
	var lines: Array[String] = []
	for player_id in _last_players:
		var player = _last_players[player_id]
		var line := "%s — fichas %d" % [_display_name(player["player_id"]), player["balance"]]
		var last_round = player["last_round"]
		if not last_round.is_empty():
			var outcome_text := "ganó" if last_round["win"] else "perdió"
			line += " — última bola: slot %d/%d, x%.2f, %s" % [
				last_round["slot"], last_round["rows"], last_round["multiplier"], outcome_text
			]
		lines.append(line)
	players_label.text = "\n".join(lines)
```

Guardar en `scenes/plinko_table_net.gd`. (Indentado con tabs, igual que `dice_table_net.gd`/`roulette_table_net.gd` — es un script de escena, no lógica pura.)

- [ ] **Step 2: Crear la escena en el editor de Godot**

`scenes/plinko_table_net.tscn`: raíz `Control` (`anchors_preset = 15`, `anchor_right = 1.0`, `anchor_bottom = 1.0`, `grow_horizontal = 2`, `grow_vertical = 2`, mismo patrón que `dice_table_net.tscn`), hijos:
- `TableController` (`Node`, script `scripts/net/plinko_table_controller.gd`)
- `Label` `PlayersLabel` (`offset_left=20, offset_top=20, offset_right=700, offset_bottom=140`, texto inicial "Nadie ha soltado la bola todavía.")
- `SpinBox` `RowsSpinBox` (`offset_left=20, offset_top=160, offset_right=160, offset_bottom=196`, `min_value=8`, `max_value=16`, `value=12`)
- `SpinBox` `AmountSpinBox` (`offset_left=180, offset_top=160, offset_right=320, offset_bottom=196`, `min_value=1`, `max_value=500`, `step=10`, `value=50`)
- `Button` `DropButton` (`offset_left=340, offset_top=160, offset_right=560, offset_bottom=196`, texto "Dejar caer bola")

Adjuntar `scenes/plinko_table_net.gd` a la raíz. `DropButton` lleva 220px de ancho — "Dejar caer bola" (16 caracteres) cabe con margen sin tocar el gotcha de Ruleta (commit `cb50b8e`).

- [ ] **Step 3: Añadir la mesa a CasinoFloor y conectar chips_won para la meta colectiva**

Editar `scenes/casino_floor.tscn`: añadir el `ext_resource` de `plinko_table_net.tscn` (id `"7"`, sube `load_steps` a `8`), instanciar `PlinkoTableNet` en su propia franja vertical debajo de Dice (que ocupa 1100-1400), y recorrer 340px hacia abajo `GoalLabel`/`UnlockedBanner`/`BattleStatusLabel` para dejarle sitio:

```
[node name="PlinkoTableNet" parent="." instance=ExtResource("7")]
anchor_bottom = 0.0
offset_top = 1420.0
offset_bottom = 1740.0
```

`GoalLabel` pasa de `offset_top=1420/offset_bottom=1444` a `offset_top=1760/offset_bottom=1784`; `UnlockedBanner` pasa de `1456/1490` a `1796/1830`; `BattleStatusLabel` pasa de `1420/1490` a `1760/1830`.

Editar `scripts/net/casino_floor.gd`: en el bloque `else` de `_ready()` (modo libre, no batalla), donde ya se conecta `chips_won` de las mesas `TableController`/`DiceTableController` a la meta colectiva, añadir el mismo hook para `PlinkoTableController`:

```gdscript
for controller in find_children("*", "TableController", true, false):
	controller.chips_won.connect(_on_chips_won)
for controller in find_children("*", "DiceTableController", true, false):
	controller.chips_won.connect(_on_chips_won)
for controller in find_children("*", "PlinkoTableController", true, false):
	controller.chips_won.connect(_on_chips_won)
```

- [ ] **Step 4: Verificación manual con dos cuentas (flujo completo)** (⚠️ requiere dos cuentas Steam en vivo, no se puede automatizar)

Repite el flujo de invitación ya operativo (A crea partida, invita a B, B acepta) hasta llegar a `CasinoFloor` en ambas instancias. En A: en la mesa de Plinko (franja inferior, debajo de Dice), fijar filas y monto en los `SpinBox`, pulsar "Dejar caer bola". En B: fijar otro monto, pulsar "Dejar caer bola". Confirmar que en **ambas** pantallas aparecen las dos tiradas (la propia y la del otro jugador) con su slot/multiplicador/gana-o-pierde, que el balance de cada uno cambia igual en las dos instancias, y que Blackjack/Ruleta/Póker/Dice siguen funcionando en paralelo sin interferencias. Si no está en modo batalla, confirmar también que una tirada con pago neto positivo suma a "Meta colectiva" (`GoalLabel`) en ambas pantallas.

- [ ] **Step 5: Commit**

```bash
git add scenes/plinko_table_net.gd scenes/plinko_table_net.tscn scenes/casino_floor.tscn scripts/net/casino_floor.gd
git commit -m "feat: add PlinkoTableNet scene and integrate into CasinoFloor"
```
