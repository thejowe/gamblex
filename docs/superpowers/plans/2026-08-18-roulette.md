# Ruleta (Plan 5) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Añadir el módulo de Ruleta europea (números 0-36, apuestas pleno/color/par-impar/docena, giro con RNG, pago según tipo de apuesta) como una mesa multijugador más en `CasinoFloor`, siguiendo exactamente el mismo patrón `GameLogic` (lógica pura testeable) + `TableController` (puente RPC) que Blackjack usa en Plan 1/Plan 3.

**Architecture:** `RouletteWheel` (lógica pura, RNG inyectable/reemplazable en tests, análoga a `Deck`) genera el número ganador. `RouletteTableState` (lógica pura, multi-asiento, vive solo en el host, análoga a `BlackjackTableState`) gestiona asientos, apuestas por tipo, el giro y la resolución de pagos vía `ChipLedger`. `RouletteTableController` (nodo de red, análogo a `TableController` de Plan 3) recibe acciones de los clientes por RPC, las valida contra `RouletteTableState`, y retransmite el estado resultante a todos — incluye el mismo wrapper host-vs-cliente y `request_state()` para clientes que entran tarde que ya están resueltos en `TableController`. La escena `RouletteTableNet` aloja la mesa y se añade a `CasinoFloor` junto a `BlackjackTableNet`.

**Tech Stack:** Godot 4.4+, GDScript, GUT (lógica de `RouletteWheel`/`RouletteTableState`, sin red), `MultiplayerAPI`/RPCs nativos de Godot sobre el `SteamMultiplayerPeer` ya operativo (Plan 2/3).

**Spec:** `docs/superpowers/specs/2026-08-17-casino-multiplayer-design.md`

## Global Constraints

- No se toca `scripts/net/table_controller.gd`, `scripts/blackjack/*` ni `scenes/blackjack_table*` — son de Blackjack (Plan 1/3), la Ruleta añade sus propias clases y escenas paralelas, no reutiliza ni modifica las de Blackjack.
- Servidor autoritativo: toda decisión de juego (girar la ruleta, validar apuesta, resolver pago) ocurre solo en el host (`multiplayer.is_server()`); los clientes solo envían intención y reciben estado.
- El servidor tiene siempre `unique_id == 1` en el `MultiplayerAPI` de Godot — los clientes dirigen sus RPCs de acción con `rpc_id(1, ...)`.
- Gotcha ya resuelto en Plan 3, replicar el mismo patrón: `rpc_id(1, ...)` con modo `call_remote` falla si quien llama ya es el peer 1 (el host actuando sobre su propia mesa). `RouletteTableController` expone métodos wrapper (`sit`, `place_bet`, `spin`) que en el host aplican la acción directamente al estado en vez de auto-llamarse por RPC; los clientes sí pasan por las RPCs `request_*`.
- Mesa con 4 asientos fijos (`SEAT_COUNT = 4`), igual que Blackjack.
- Ruleta europea: 37 números (0-36), un solo cero, sin doble cero.
- Sin persistencia entre partidas — el estado de la mesa vive solo en memoria del host mientras dura la sesión.

## Nota sobre testing en este plan

`RouletteWheel` y `RouletteTableState` son lógica pura (Tasks 1-3) y se testean con GUT sin red, igual que `BlackjackTableState` en Plan 3. `RouletteTableController` y la escena (Tasks 4-5) dependen de una sesión multijugador real ya establecida y se verifican manualmente con dos instancias — mismo criterio que Plan 3.

---

## Task 1: RouletteWheel — giro con resultado inyectable para tests

**Files:**
- Create: `scripts/roulette/roulette_wheel.gd`
- Test: `tests/unit/test_roulette_wheel.gd`

**Interfaces:**
- Produce: `RouletteWheel.new()`, propiedad `results: Array[int]` (cola de resultados forzados, vacía por defecto), método `spin() -> int` — si `results` no está vacío, hace `pop_front()` y lo devuelve; si está vacío, devuelve un entero aleatorio `0..36`. Usado por `RouletteTableState` (Task 2).

- [ ] **Step 1: Escribir los tests**

```gdscript
extends GutTest

func test_spin_returns_queued_result_in_order():
    var wheel = RouletteWheel.new()
    wheel.results = [7, 0, 36]
    assert_eq(wheel.spin(), 7)
    assert_eq(wheel.spin(), 0)
    assert_eq(wheel.spin(), 36)

func test_spin_without_queue_returns_number_in_range():
    var wheel = RouletteWheel.new()
    for i in range(50):
        var result = wheel.spin()
        assert_true(result >= 0 and result <= 36)
```

Guardar en `tests/unit/test_roulette_wheel.gd`.

- [ ] **Step 2: Ejecutar y verificar que fallan**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`
Expected: FAIL — `Identifier "RouletteWheel" not declared`.

- [ ] **Step 3: Implementar RouletteWheel**

```gdscript
class_name RouletteWheel
extends RefCounted

var results: Array[int] = []

func spin() -> int:
    if not results.is_empty():
        return results.pop_front()
    return randi() % 37
```

Guardar en `scripts/roulette/roulette_wheel.gd`.

- [ ] **Step 4: Ejecutar y verificar que pasan**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`
Expected: todos los tests de `test_roulette_wheel.gd` en verde, `0 failed`.

- [ ] **Step 5: Commit**

```bash
git add scripts/roulette/roulette_wheel.gd tests/unit/test_roulette_wheel.gd
git commit -m "feat: add RouletteWheel with test-injectable results queue"
```

---

## Task 2: RouletteTableState — asientos y apuestas

**Files:**
- Create: `scripts/roulette/roulette_table_state.gd`
- Test: `tests/unit/test_roulette_table_state.gd`

**Interfaces:**
- Consume: `ChipLedger` (Plan 1, `scripts/chip_ledger.gd`), `RouletteWheel` (Task 1, `scripts/roulette/roulette_wheel.gd`).
- Produce: `RouletteTableState.new(wheel: RouletteWheel = null)`, constante `SEAT_COUNT = 4`, enum `BetType { STRAIGHT, RED, BLACK, EVEN, ODD, DOZEN_1, DOZEN_2, DOZEN_3 }` (nested, accesible como `RouletteTableState.BetType.RED`), propiedad `seats: Array` (tamaño 4, cada elemento `null` o un objeto `Seat` con `player_id: int`, `ledger: ChipLedger`, `bets: Array`), método `sit(seat_index: int, player_id: int) -> bool`, `place_bet(seat_index: int, player_id: int, bet_type: int, number: int, amount: int) -> bool` — `number` solo se usa cuando `bet_type == BetType.STRAIGHT` (rango 0-36), se ignora en el resto. Usado por Task 3 (mismo archivo, añade `spin`/resolución) y por `RouletteTableController` (Task 4).

- [ ] **Step 1: Escribir los tests**

```gdscript
extends GutTest

func test_sit_occupies_empty_seat():
    var table = RouletteTableState.new()
    var ok = table.sit(0, 111)
    assert_true(ok)
    assert_eq(table.seats[0].player_id, 111)

func test_sit_fails_on_occupied_seat():
    var table = RouletteTableState.new()
    table.sit(0, 111)
    var ok = table.sit(0, 222)
    assert_false(ok)

func test_sit_fails_if_player_already_seated_elsewhere():
    var table = RouletteTableState.new()
    table.sit(0, 111)
    var ok = table.sit(1, 111)
    assert_false(ok)

func test_sit_fails_on_out_of_range_seat():
    var table = RouletteTableState.new()
    var ok = table.sit(4, 111)
    assert_false(ok)

func test_place_bet_deducts_balance_and_records_bet():
    var table = RouletteTableState.new()
    table.sit(0, 111)
    var ok = table.place_bet(0, 111, RouletteTableState.BetType.RED, -1, 50)
    assert_true(ok)
    assert_eq(table.seats[0].ledger.balance, 450)
    assert_eq(table.seats[0].bets.size(), 1)

func test_place_bet_allows_multiple_bets_same_seat():
    var table = RouletteTableState.new()
    table.sit(0, 111)
    table.place_bet(0, 111, RouletteTableState.BetType.RED, -1, 50)
    table.place_bet(0, 111, RouletteTableState.BetType.STRAIGHT, 7, 100)
    assert_eq(table.seats[0].ledger.balance, 350)
    assert_eq(table.seats[0].bets.size(), 2)

func test_place_bet_fails_for_unseated_or_wrong_player():
    var table = RouletteTableState.new()
    table.sit(0, 111)
    var ok = table.place_bet(0, 999, RouletteTableState.BetType.RED, -1, 50)
    assert_false(ok)
    assert_eq(table.seats[0].bets.size(), 0)

func test_place_bet_fails_on_insufficient_balance():
    var table = RouletteTableState.new()
    table.sit(0, 111)
    var ok = table.place_bet(0, 111, RouletteTableState.BetType.RED, -1, 5000)
    assert_false(ok)
    assert_eq(table.seats[0].ledger.balance, 500)

func test_place_bet_fails_on_invalid_bet_type():
    var table = RouletteTableState.new()
    table.sit(0, 111)
    var ok = table.place_bet(0, 111, 999, -1, 50)
    assert_false(ok)

func test_place_bet_straight_fails_on_out_of_range_number():
    var table = RouletteTableState.new()
    table.sit(0, 111)
    var ok = table.place_bet(0, 111, RouletteTableState.BetType.STRAIGHT, 37, 50)
    assert_false(ok)
```

Guardar en `tests/unit/test_roulette_table_state.gd`.

- [ ] **Step 2: Ejecutar y verificar que fallan**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`
Expected: FAIL — `Identifier "RouletteTableState" not declared`.

- [ ] **Step 3: Implementar RouletteTableState (parte 1: asientos y apuestas)**

```gdscript
class_name RouletteTableState
extends RefCounted

const SEAT_COUNT := 4
const NUMBER_COUNT := 37 # 0-36

enum BetType { STRAIGHT, RED, BLACK, EVEN, ODD, DOZEN_1, DOZEN_2, DOZEN_3 }

class Bet:
    var type: int
    var number: int
    var amount: int

class Seat:
    var player_id: int = 0
    var ledger: ChipLedger
    var bets: Array = []

var seats: Array = []
var wheel: RouletteWheel
var last_result: int = -1

func _init(p_wheel: RouletteWheel = null) -> void:
    wheel = p_wheel if p_wheel else RouletteWheel.new()
    for i in range(SEAT_COUNT):
        seats.append(null)

func sit(seat_index: int, player_id: int) -> bool:
    if seat_index < 0 or seat_index >= SEAT_COUNT:
        return false
    if seats[seat_index] != null:
        return false
    for s in seats:
        if s != null and s.player_id == player_id:
            return false
    var seat := Seat.new()
    seat.player_id = player_id
    seat.ledger = ChipLedger.new(500)
    seats[seat_index] = seat
    return true

func place_bet(seat_index: int, player_id: int, bet_type: int, number: int, amount: int) -> bool:
    var seat = _seat_for(seat_index, player_id)
    if seat == null:
        return false
    if not _is_valid_bet_type(bet_type):
        return false
    if bet_type == BetType.STRAIGHT and (number < 0 or number >= NUMBER_COUNT):
        return false
    if not seat.ledger.place_bet(amount):
        return false
    var bet := Bet.new()
    bet.type = bet_type
    bet.number = number if bet_type == BetType.STRAIGHT else -1
    bet.amount = amount
    seat.bets.append(bet)
    return true

func _is_valid_bet_type(bet_type: int) -> bool:
    return bet_type >= BetType.STRAIGHT and bet_type <= BetType.DOZEN_3

func _seat_for(seat_index: int, player_id: int):
    if seat_index < 0 or seat_index >= SEAT_COUNT:
        return null
    var seat = seats[seat_index]
    if seat == null or seat.player_id != player_id:
        return null
    return seat
```

Guardar en `scripts/roulette/roulette_table_state.gd`. `spin`/resolución de pagos/`to_dict` se añaden en Task 3 — con esto ya deberían pasar los tests de Task 2.

- [ ] **Step 4: Ejecutar y verificar que pasan**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`
Expected: todos los tests de `test_roulette_table_state.gd` en verde, `0 failed`.

- [ ] **Step 5: Commit**

```bash
git add scripts/roulette/roulette_table_state.gd tests/unit/test_roulette_table_state.gd
git commit -m "feat: add RouletteTableState seating and multi-bet placement"
```

---

## Task 3: RouletteTableState — giro y resolución de pagos

**Files:**
- Modify: `scripts/roulette/roulette_table_state.gd`
- Modify: `tests/unit/test_roulette_table_state.gd`

**Interfaces:**
- Produce (añadido a `RouletteTableState`): `spin(seat_index: int, player_id: int) -> bool` (cualquier jugador sentado puede disparar el giro; resuelve **todas** las apuestas de **todos** los asientos, no solo las del que giró), `to_dict() -> Dictionary`.

- [ ] **Step 1: Añadir los tests de giro y resolución**

Añadir al final de `tests/unit/test_roulette_table_state.gd`:

```gdscript
func test_spin_resolves_straight_win_at_35_to_1():
    var wheel = RouletteWheel.new()
    wheel.results = [7]
    var table = RouletteTableState.new(wheel)
    table.sit(0, 111)
    table.place_bet(0, 111, RouletteTableState.BetType.STRAIGHT, 7, 100)
    var ok = table.spin(0, 111)
    assert_true(ok)
    assert_eq(table.last_result, 7)
    assert_eq(table.seats[0].ledger.balance, 500 - 100 + 3600) # 100 * 36

func test_spin_resolves_straight_loss():
    var wheel = RouletteWheel.new()
    wheel.results = [8]
    var table = RouletteTableState.new(wheel)
    table.sit(0, 111)
    table.place_bet(0, 111, RouletteTableState.BetType.STRAIGHT, 7, 100)
    table.spin(0, 111)
    assert_eq(table.seats[0].ledger.balance, 400)

func test_spin_resolves_color_bet_win_and_loss():
    var wheel = RouletteWheel.new()
    wheel.results = [1] # 1 es rojo
    var table = RouletteTableState.new(wheel)
    table.sit(0, 111)
    table.sit(1, 222)
    table.place_bet(0, 111, RouletteTableState.BetType.RED, -1, 50)
    table.place_bet(1, 222, RouletteTableState.BetType.BLACK, -1, 50)
    table.spin(0, 111)
    assert_eq(table.seats[0].ledger.balance, 500 - 50 + 100) # gana 1:1
    assert_eq(table.seats[1].ledger.balance, 450) # pierde

func test_spin_resolves_even_odd_bet():
    var wheel = RouletteWheel.new()
    wheel.results = [4]
    var table = RouletteTableState.new(wheel)
    table.sit(0, 111)
    table.sit(1, 222)
    table.place_bet(0, 111, RouletteTableState.BetType.EVEN, -1, 50)
    table.place_bet(1, 222, RouletteTableState.BetType.ODD, -1, 50)
    table.spin(0, 111)
    assert_eq(table.seats[0].ledger.balance, 500 - 50 + 100)
    assert_eq(table.seats[1].ledger.balance, 450)

func test_spin_resolves_dozen_bet_at_2_to_1():
    var wheel = RouletteWheel.new()
    wheel.results = [15] # segunda docena, 13-24
    var table = RouletteTableState.new(wheel)
    table.sit(0, 111)
    table.place_bet(0, 111, RouletteTableState.BetType.DOZEN_2, -1, 50)
    table.spin(0, 111)
    assert_eq(table.seats[0].ledger.balance, 500 - 50 + 150) # 50 * 3

func test_spin_on_zero_only_straight_zero_wins():
    var wheel = RouletteWheel.new()
    wheel.results = [0]
    var table = RouletteTableState.new(wheel)
    table.sit(0, 111)
    table.sit(1, 222)
    table.place_bet(0, 111, RouletteTableState.BetType.RED, -1, 50)
    table.place_bet(1, 222, RouletteTableState.BetType.STRAIGHT, 0, 50)
    table.spin(0, 111)
    assert_eq(table.seats[0].ledger.balance, 450) # rojo pierde en el 0
    assert_eq(table.seats[1].ledger.balance, 500 - 50 + 1800) # pleno al 0 gana

func test_spin_resolves_multiple_bets_same_seat_independently():
    var wheel = RouletteWheel.new()
    wheel.results = [7] # rojo, impar
    var table = RouletteTableState.new(wheel)
    table.sit(0, 111)
    table.place_bet(0, 111, RouletteTableState.BetType.RED, -1, 50) # gana
    table.place_bet(0, 111, RouletteTableState.BetType.EVEN, -1, 50) # pierde
    table.spin(0, 111)
    assert_eq(table.seats[0].ledger.balance, 500 - 100 + 100)

func test_spin_clears_bets_after_resolution():
    var wheel = RouletteWheel.new()
    wheel.results = [7]
    var table = RouletteTableState.new(wheel)
    table.sit(0, 111)
    table.place_bet(0, 111, RouletteTableState.BetType.RED, -1, 50)
    table.spin(0, 111)
    assert_eq(table.seats[0].bets.size(), 0)

func test_spin_fails_for_unseated_player():
    var table = RouletteTableState.new()
    var ok = table.spin(0, 999)
    assert_false(ok)
    assert_eq(table.last_result, -1)

func test_to_dict_reflects_seats_bets_and_last_result():
    var wheel = RouletteWheel.new()
    wheel.results = [7]
    var table = RouletteTableState.new(wheel)
    table.sit(0, 111)
    table.place_bet(0, 111, RouletteTableState.BetType.RED, -1, 50)
    table.spin(0, 111)
    var data = table.to_dict()
    assert_eq(data["seats"][0]["player_id"], 111)
    assert_eq(data["seats"][1], null)
    assert_eq(data["last_result"], 7)
    assert_eq(data["seats"][0]["bets"].size(), 0)
```

- [ ] **Step 2: Ejecutar y verificar que fallan**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`
Expected: FAIL — `Invalid call. Nonexistent function 'spin'/'to_dict'`.

- [ ] **Step 3: Implementar spin/resolución/to_dict**

Añadir dentro de `scripts/roulette/roulette_table_state.gd`:

```gdscript
const RED_NUMBERS: Array[int] = [1, 3, 5, 7, 9, 12, 14, 16, 18, 19, 21, 23, 25, 27, 30, 32, 34, 36]

func spin(seat_index: int, player_id: int) -> bool:
    var seat = _seat_for(seat_index, player_id)
    if seat == null:
        return false
    var result := wheel.spin()
    last_result = result
    for s in seats:
        if s == null:
            continue
        for bet in s.bets:
            _resolve_bet(s, bet, result)
        s.bets.clear()
    return true

func _resolve_bet(seat, bet, result: int) -> void:
    if _bet_wins(bet, result):
        seat.ledger.payout(bet.amount * (_payout_multiplier(bet.type) + 1))

func _bet_wins(bet, result: int) -> bool:
    match bet.type:
        BetType.STRAIGHT:
            return bet.number == result
        BetType.RED:
            return result != 0 and result in RED_NUMBERS
        BetType.BLACK:
            return result != 0 and not (result in RED_NUMBERS)
        BetType.EVEN:
            return result != 0 and result % 2 == 0
        BetType.ODD:
            return result != 0 and result % 2 == 1
        BetType.DOZEN_1:
            return result >= 1 and result <= 12
        BetType.DOZEN_2:
            return result >= 13 and result <= 24
        BetType.DOZEN_3:
            return result >= 25 and result <= 36
    return false

func _payout_multiplier(bet_type: int) -> int:
    match bet_type:
        BetType.STRAIGHT:
            return 35
        BetType.RED, BetType.BLACK, BetType.EVEN, BetType.ODD:
            return 1
        BetType.DOZEN_1, BetType.DOZEN_2, BetType.DOZEN_3:
            return 2
    return 0

func to_dict() -> Dictionary:
    var seats_data := []
    for seat in seats:
        if seat == null:
            seats_data.append(null)
        else:
            var bets_data := []
            for bet in seat.bets:
                bets_data.append({"type": bet.type, "number": bet.number, "amount": bet.amount})
            seats_data.append({
                "player_id": seat.player_id,
                "balance": seat.ledger.balance,
                "bets": bets_data,
            })
    return {
        "seats": seats_data,
        "last_result": last_result,
    }
```

- [ ] **Step 4: Ejecutar y verificar que pasan**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`
Expected: todos los tests en verde, `0 failed`, incluidos los de Task 2.

- [ ] **Step 5: Commit**

```bash
git add scripts/roulette/roulette_table_state.gd tests/unit/test_roulette_table_state.gd
git commit -m "feat: add spin resolution and payouts to RouletteTableState"
```

---

## Task 4: RouletteTableController — puente de red sobre RouletteTableState

**Files:**
- Create: `scripts/net/roulette_table_controller.gd`

**Interfaces:**
- Consume: `RouletteTableState` (Task 2/3 completo), `multiplayer.is_server()`, `multiplayer.get_remote_sender_id()`, `multiplayer.get_unique_id()` (API estándar de Godot).
- Produce: nodo `RouletteTableController` con señal `state_changed(state: Dictionary)`, métodos de entrada `sit(seat_index: int)`, `place_bet(seat_index: int, bet_type: int, number: int, amount: int)`, `spin(seat_index: int)`, `request_state()` — cada uno aplica la acción directamente si es el host, o hace `rpc_id(1, ...)` si es cliente (mismo wrapper que `TableController` de Plan 3). Usado por `RouletteTableNet` (Task 5).

- [ ] **Step 1: Implementar RouletteTableController**

```gdscript
class_name RouletteTableController
extends Node

signal state_changed(state: Dictionary)

var table_state: RouletteTableState

func _ready() -> void:
    if multiplayer.is_server():
        table_state = RouletteTableState.new()

# Puntos de entrada para la UI local (host o cliente). El host actúa directo
# sobre table_state; un rpc_id(1, ...) a sí mismo lo rechaza el MultiplayerAPI
# ("call_remote" no permite invocarse a uno mismo), así que el host no puede
# pasar por las RPCs request_* para sus propias acciones.
func sit(seat_index: int) -> void:
    if multiplayer.is_server():
        _apply_sit(seat_index, multiplayer.get_unique_id())
    else:
        request_sit.rpc_id(1, seat_index)

func place_bet(seat_index: int, bet_type: int, number: int, amount: int) -> void:
    if multiplayer.is_server():
        _apply_bet(seat_index, bet_type, number, amount, multiplayer.get_unique_id())
    else:
        request_bet.rpc_id(1, seat_index, bet_type, number, amount)

func spin(seat_index: int) -> void:
    if multiplayer.is_server():
        _apply_spin(seat_index, multiplayer.get_unique_id())
    else:
        request_spin.rpc_id(1, seat_index)

# Un cliente que entra a la mesa después de que ya hubo cambios (alguien
# sentado, apuestas hechas) no recibe nada por su cuenta: el host solo
# retransmite _receive_state cuando algo cambia, nunca al conectar. Sin este
# pedido explícito el cliente se queda con el estado en blanco para siempre.
func request_state() -> void:
    if multiplayer.is_server():
        return
    _request_state.rpc_id(1)

@rpc("any_peer", "call_remote", "reliable")
func _request_state() -> void:
    if not multiplayer.is_server():
        return
    _receive_state.rpc_id(multiplayer.get_remote_sender_id(), table_state.to_dict())

@rpc("any_peer", "call_remote", "reliable")
func request_sit(seat_index: int) -> void:
    if not multiplayer.is_server():
        return
    _apply_sit(seat_index, multiplayer.get_remote_sender_id())

@rpc("any_peer", "call_remote", "reliable")
func request_bet(seat_index: int, bet_type: int, number: int, amount: int) -> void:
    if not multiplayer.is_server():
        return
    _apply_bet(seat_index, bet_type, number, amount, multiplayer.get_remote_sender_id())

@rpc("any_peer", "call_remote", "reliable")
func request_spin(seat_index: int) -> void:
    if not multiplayer.is_server():
        return
    _apply_spin(seat_index, multiplayer.get_remote_sender_id())

func _apply_sit(seat_index: int, player_id: int) -> void:
    if table_state.sit(seat_index, player_id):
        _broadcast_state()

func _apply_bet(seat_index: int, bet_type: int, number: int, amount: int, player_id: int) -> void:
    if table_state.place_bet(seat_index, player_id, bet_type, number, amount):
        _broadcast_state()

func _apply_spin(seat_index: int, player_id: int) -> void:
    if table_state.spin(seat_index, player_id):
        _broadcast_state()

func _broadcast_state() -> void:
    _receive_state.rpc(table_state.to_dict())

@rpc("authority", "call_local", "reliable")
func _receive_state(state: Dictionary) -> void:
    state_changed.emit(state)
```

Guardar en `scripts/net/roulette_table_controller.gd`. Nota: en el host, `_ready()` crea `table_state`; en los clientes queda `null` a propósito — los clientes nunca ejecutan lógica de juego, solo reciben `state_changed` y piden acciones por RPC.

- [ ] **Step 2: Verificación manual con dos cuentas**

Reutilizando el flujo de lobby ya operativo (Plan 2/3), añade temporalmente un `RouletteTableController` como nodo hijo de la escena principal en ambas instancias y conecta:

```gdscript
$RouletteTableController.state_changed.connect(func(state): print("Estado ruleta: %s" % state))
```

Desde la instancia cliente, llama `$RouletteTableController.sit(0)`, luego `$RouletteTableController.place_bet(0, RouletteTableState.BetType.RED, -1, 50)`, luego `$RouletteTableController.spin(0)`. Confirma que **ambas** instancias imprimen el mismo `Estado ruleta` tras cada acción (la del host porque es la fuente, la del cliente porque le llegó por `_receive_state`), y que `last_result` y el balance del asiento cambian igual en las dos.

- [ ] **Step 3: Commit**

```bash
git add scripts/net/roulette_table_controller.gd
git commit -m "feat: add RouletteTableController RPC bridge for RouletteTableState"
```

---

## Task 5: Escena RouletteTableNet + integración en CasinoFloor

**Files:**
- Create: `scenes/roulette_table_net.gd`
- Create: `scenes/roulette_table_net.tscn`
- Modify: `scenes/casino_floor.tscn`

**Interfaces:**
- Consume: `RouletteTableController` (Task 4), `RouletteTableState.BetType` (Task 2), `NetworkManager.peer_steam_ids`/`identities_changed` (Plan 2, ya usado por `blackjack_table_net.gd`).

- [ ] **Step 1: Escribir el script de la mesa jugable**

```gdscript
extends Control

@onready var table_controller: RouletteTableController = $TableController
@onready var seats_label: Label = $SeatsLabel
@onready var result_label: Label = $ResultLabel
@onready var sit_button: Button = $SitButton
@onready var bet_red_button: Button = $BetRedButton
@onready var bet_black_button: Button = $BetBlackButton
@onready var bet_even_button: Button = $BetEvenButton
@onready var bet_odd_button: Button = $BetOddButton
@onready var bet_straight_button: Button = $BetStraightButton
@onready var spin_button: Button = $SpinButton

var my_seat_index: int = -1
var _last_seats: Array = []

func _display_name(peer_id: int) -> String:
	var steam_id: int = NetworkManager.peer_steam_ids.get(peer_id, 0)
	if steam_id == 0:
		return "jugador %d" % peer_id
	var persona_name := Steam.getFriendPersonaName(steam_id)
	return persona_name if not persona_name.is_empty() else "jugador %d" % peer_id

func _ready() -> void:
	table_controller.state_changed.connect(_on_state_changed)
	sit_button.pressed.connect(_on_sit_pressed)
	bet_red_button.pressed.connect(func(): _place_bet(RouletteTableState.BetType.RED, -1, 50))
	bet_black_button.pressed.connect(func(): _place_bet(RouletteTableState.BetType.BLACK, -1, 50))
	bet_even_button.pressed.connect(func(): _place_bet(RouletteTableState.BetType.EVEN, -1, 50))
	bet_odd_button.pressed.connect(func(): _place_bet(RouletteTableState.BetType.ODD, -1, 50))
	bet_straight_button.pressed.connect(func(): _place_bet(RouletteTableState.BetType.STRAIGHT, 7, 100))
	spin_button.pressed.connect(_on_spin_pressed)
	NetworkManager.identities_changed.connect(_refresh_seats_label)
	# Mismo gotcha que blackjack_table_net.gd: un cliente recién llegado a esta
	# escena puede que aún no tenga el SteamMultiplayerPeer en CONNECTED —
	# bloquear "Sentarse" hasta entonces evita el rpc_id() fallido.
	if not multiplayer.is_server():
		var peer := multiplayer.multiplayer_peer
		if peer == null or peer.get_connection_status() != MultiplayerPeer.CONNECTION_CONNECTED:
			sit_button.disabled = true
			multiplayer.connected_to_server.connect(func():
				sit_button.disabled = false
				table_controller.request_state()
			)
		else:
			table_controller.request_state()

func _on_sit_pressed() -> void:
	var seat_index := 0
	for i in range(_last_seats.size()):
		if _last_seats[i] == null:
			seat_index = i
			break
	table_controller.sit(seat_index)
	my_seat_index = seat_index

func _place_bet(bet_type: int, number: int, amount: int) -> void:
	table_controller.place_bet(my_seat_index, bet_type, number, amount)

func _on_spin_pressed() -> void:
	table_controller.spin(my_seat_index)

func _on_state_changed(state: Dictionary) -> void:
	_last_seats = state["seats"]
	result_label.text = "Último número: %d" % state["last_result"]
	_refresh_seats_label()

func _refresh_seats_label() -> void:
	var lines: Array[String] = []
	for i in range(_last_seats.size()):
		var seat = _last_seats[i]
		if seat == null:
			lines.append("Asiento %d: libre" % i)
		else:
			lines.append("Asiento %d: %s — fichas %d — apuestas activas %d" % [
				i, _display_name(seat["player_id"]), seat["balance"], seat["bets"].size()
			])
	seats_label.text = "\n".join(lines)
```

Guardar en `scenes/roulette_table_net.gd`. (Indentado con tabs, igual que `blackjack_table_net.gd` — es un script de escena, no lógica pura.)

- [ ] **Step 2: Crear la escena en el editor de Godot**

`scenes/roulette_table_net.tscn`: raíz `Control`, hijos: `TableController` (nodo `Node` con el script de Task 4, `scripts/net/roulette_table_controller.gd`), `Label` `SeatsLabel`, `Label` `ResultLabel` (texto inicial "Último número: -1"), `Button` `SitButton` ("Sentarse"), `Button` `BetRedButton` ("Apostar Rojo 50"), `Button` `BetBlackButton` ("Apostar Negro 50"), `Button` `BetEvenButton` ("Apostar Par 50"), `Button` `BetOddButton` ("Apostar Impar 50"), `Button` `BetStraightButton` ("Apostar Pleno 7 (100)"), `Button` `SpinButton` ("Girar"). Adjuntar `scenes/roulette_table_net.gd` a la raíz.

- [ ] **Step 3: Añadir la mesa a CasinoFloor**

Editar `scenes/casino_floor.tscn` para instanciar `RouletteTableNet` junto a `BlackjackTableNet`, y repartir el ancho de pantalla entre las dos mesas (mitad izquierda Blackjack, mitad derecha Ruleta) para que no se solapen:

```
[gd_scene load_steps=3 format=3]

[ext_resource type="PackedScene" path="res://scenes/blackjack_table_net.tscn" id="1"]
[ext_resource type="PackedScene" path="res://scenes/roulette_table_net.tscn" id="2"]

[node name="CasinoFloor" type="Node2D"]

[node name="BlackjackTableNet" parent="." instance=ExtResource("1")]
anchor_right = 0.5

[node name="RouletteTableNet" parent="." instance=ExtResource("2")]
anchor_left = 0.5
anchor_right = 1.0
```

- [ ] **Step 4: Verificación manual con dos cuentas (flujo completo)**

Repite el flujo de invitación ya operativo (A crea partida, invita a B, B acepta) hasta llegar a `CasinoFloor` en ambas instancias. En A: en la mesa de Ruleta (mitad derecha), pulsar "Sentarse", luego "Apostar Rojo 50" y "Apostar Pleno 7 (100)". En B: pulsar "Sentarse" (ocupa el siguiente asiento libre automáticamente), luego "Apostar Negro 50". Cualquiera de los dos pulsa "Girar". Confirmar que en **ambas** pantallas aparece el mismo "Último número", que las fichas de cada asiento cambian según ganó/perdió esa tirada, y que las apuestas quedan en 0 tras el giro — visible igual en las dos instancias. Confirmar también que la mesa de Blackjack sigue funcionando en paralelo sin interferencias.

- [ ] **Step 5: Commit**

```bash
git add scenes/roulette_table_net.gd scenes/roulette_table_net.tscn scenes/casino_floor.tscn
git commit -m "feat: add RouletteTableNet scene and integrate into CasinoFloor"
```
</content>
