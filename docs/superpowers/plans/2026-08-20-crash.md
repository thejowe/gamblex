# Crash Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Añadir el módulo Crash (Plan 9) — multiplicador creciente en tiempo real con cash-out voluntario y punto de explosión decidido por el host — reutilizando el patrón de "ronda independiente por jugador" fijado por Plan 8 (Dice), e integrarlo en `CasinoFloor`.

**Architecture:** Tres capas RefCounted/Node siguiendo exactamente el patrón `Dice*`:
`CrashRoller` (RNG del host, formula del punto de explosión, injectable para tests) →
`CrashTableState` (lógica pura sin red: apostar, retirar, avance de tiempo por-jugador vía
`advance_time(delta)` al estilo `MatchRules.advance_time`, sin `SEAT_COUNT`) →
`CrashTableController` (wrapper host-vs-cliente con RPCs, más un `_process(delta)` que sólo
corre en el host para hacer avanzar el tiempo y detectar explosiones). La UI cliente extrapola
localmente el multiplicador en pantalla desde el `elapsed` recibido en el último broadcast +
tiempo local transcurrido — el host nunca envía el `crash_point` de una ronda activa (solo tras
resolverse), así ningún cliente puede leer por adelantado cuándo va a explotar.

**Tech Stack:** Godot 4.4+, GDScript, GUT (`CrashRoller`/`CrashTableState`, sin red — mismo
alcance de testing que Dice), `MultiplayerAPI`/RPCs nativos sobre el `SteamMultiplayerPeer` ya
operativo (Plan 2/3).

**Spec:** `docs/superpowers/specs/2026-08-17-casino-multiplayer-design.md` (sección "Ampliación
v1.1", subsecciones "Componente nuevo: patrón de ronda independiente" y "Crash"). Interfaz base
tomada del código real de Plan 8: `scripts/dice/dice_roller.gd`, `scripts/dice/dice_table_state.gd`,
`scripts/net/dice_table_controller.gd`.

## Global Constraints

- Patrón de nombres fijado por Plan 8 para Crash/Mines/Plinko: `CrashRoller`
  (`scripts/crash/crash_roller.gd`), `CrashTableState` (`scripts/crash/crash_table_state.gd`),
  `CrashTableController` (`scripts/net/crash_table_controller.gd`) — no inventar nombres propios.
- `CrashTableState` no tiene `SEAT_COUNT` ni asientos: `players: Dictionary` (`player_id: int ->
  Player`), `_player_for(player_id)` crea la entrada perezosamente (ledger a 500 fichas,
  `STARTING_BALANCE`) la primera vez que ese jugador actúa.
- Señal `chips_won(player_id: int, amount: int)` en `CrashTableState`, emitida con la ganancia
  neta (payout menos apuesta) sólo cuando el jugador gana.
- `to_dict() -> Dictionary` con forma `{"players": {player_id: {...}}}`.
- `CrashTableController`: métodos públicos aplican la acción directo si `multiplayer.is_server()`,
  o `rpc_id(1, ...)` si no — el host nunca puede pasar por sus propias RPCs (`call_remote` rechaza
  invocarse a uno mismo), así que cada entrada pública debe ramificar exactamente así, replicando
  el comentario de `dice_table_controller.gd:14-17`.
- Señales `state_changed(state: Dictionary)` y `chips_won(player_id: int, amount: int)` en
  `CrashTableController`; `request_state()` + `_request_state`/`_receive_state` para clientes que
  entran tarde (mismo gotcha ya documentado en `dice_table_controller.gd:24-28`).
- `_broadcast_state()` hace `_receive_state.rpc(...)` a **todos** los presentes, no sólo al
  jugador que actuó.
- Punto de explosión (no cambiar la fórmula): `r = randf()` evitando `r=0`;
  `crash_point = max(1.00, floor(100 * 0.99 / (1 - r)) / 100)`.
- Curva de subida (no cambiar la forma): `multiplicador_actual(t) = 1.00 + growth_rate * t^2`.
- Controles de la UI dimensionados al texto real en español, no anchos fijos genéricos (gotcha de
  Ruleta, commit `cb50b8e`).
- Rama de trabajo: `feature/crash`. No mergear a `main` — avisar a la sesión pilar al terminar.

---

## Decisión de diseño: `growth_rate`

`r` es uniforme en `[0, 1)`. Con `r = 0.5` (mediana), `crash_point = floor(100 * 0.99 / 0.5) / 100
= 1.98x` — la ronda "típica" explota alrededor de 2x. Se fija `GROWTH_RATE = 0.02`:

- Duración mediana real: `1.98 = 1.00 + 0.02 * t^2` → `t = sqrt(0.98/0.02) = 7.00s` (no 7.07s —
  ese valor es el tiempo hasta llegar a 2.00x exacto, una cantidad ligeramente distinta a la
  mediana de duración de ronda; 7.00s cae en medio del rango 3-15s pedido de todos modos).
- `t` para 1.2x (percentil bajo, `r ∈ [0.175, 0.182)`): `t = sqrt(0.2/0.02) ≈ 3.16s` (roza el
  límite inferior).
- `t` para 5.0x (percentil alto, `r ∈ [0.802, 0.8024)`): `t = sqrt(4/0.02) ≈ 14.14s` (roza el
  límite superior).
- **Cola corta:** ~16.1% de las rondas explotan antes de los 3s (`r < 0.175`), y ~2.0% explotan
  instantáneamente en 1.00x (`r < 0.0198`, donde `floor(100*0.99/(1-r))/100` cae por debajo de
  1.00 y el `max(1.00, ...)` lo recorta al mínimo) — es donde vive de verdad el 1% de margen de
  la casa, no un caso raro. No es un bug si se observa en playtest; es el comportamiento
  esperado de la fórmula fijada por la spec.
- **Cola larga:** multiplicadores altos (10x+, ~9.9% de las rondas — `r ≥ 0.901` — por la forma
  `1/(1-r)` de la fórmula) tardan más de 15s — aceptable: la spec pide 3-15s sólo para el "rango
  medio", y una cola larga ocasional es el comportamiento esperado de un juego Crash real.

## Decisión de diseño: qué ve el cliente

El host **nunca** incluye `crash_point` de una ronda activa en el broadcast — sólo aparece en
`last_round` una vez la ronda se resolvió (por cash-out o por explosión), igual que Dice expone
`result` sólo tras la tirada. Mientras la ronda está activa el cliente sólo recibe `is_active` y
`elapsed` (el tiempo transcurrido en el momento del broadcast); la UI extrapola localmente
sumando `delta` de `_process` a partir de ahí para dibujar el multiplicador subiendo en tiempo
real, sin pedir al host que retransmita cada frame (más barato en ancho de banda, tal como permite
la spec). El host sigue siendo la única autoridad: sólo él decide de verdad cuándo explota, vía
`advance_time(delta)` corriendo en su propio `_process`.

---

## File Structure

- Create `scripts/crash/crash_roller.gd` — RNG + fórmula del punto de explosión.
- Create `scripts/crash/crash_table_state.gd` — lógica pura: apostar, retirar, avance de tiempo.
- Create `scripts/net/crash_table_controller.gd` — wrapper de red host-vs-cliente.
- Create `scenes/crash_table_net.gd` + `scenes/crash_table_net.tscn` — UI de la mesa.
- Modify `scenes/casino_floor.tscn` — añadir la franja de Crash y desplazar las etiquetas de meta
  colectiva/batalla que van debajo.
- Modify `scripts/net/casino_floor.gd` — conectar `chips_won` de `CrashTableController` a la meta
  colectiva en modo libre.
- Create `tests/unit/test_crash_roller.gd`, `tests/unit/test_crash_table_state.gd`.

---

## Task 1: CrashRoller — RNG y fórmula del punto de explosión

**Files:**
- Create: `scripts/crash/crash_roller.gd`
- Test: `tests/unit/test_crash_roller.gd`

**Interfaces:**
- Produces: `class_name CrashRoller extends RefCounted`, propiedad `results: Array[float] = []`
  (cola inyectable de **puntos de explosión ya calculados**, mismo patrón que
  `DiceRoller.results`/`RouletteWheel.results` — el que inyecta no tiene que hacer la cuenta de
  `r` a mano). Método `roll() -> float` (pop de la cola si no está vacía, si no genera `r =
  randf()` evitando `r = 0.0` y devuelve `crash_point_for(r)`). Método estático
  `crash_point_for(r: float) -> float` con la fórmula exacta de la spec, usable de forma aislada
  para testear la fórmula y para la validación estadística.

- [ ] **Step 1: Write the failing tests**

```gdscript
# tests/unit/test_crash_roller.gd
extends GutTest

func test_roll_returns_queued_result_in_order():
	var roller = CrashRoller.new()
	var queued: Array[float] = [1.50, 3.20, 1.00]
	roller.results = queued
	assert_eq(roller.roll(), 1.50)
	assert_eq(roller.roll(), 3.20)
	assert_eq(roller.roll(), 1.00)

func test_crash_point_for_known_r_values():
	# r=0.5 -> 100*0.99/0.5 = 198 -> floor/100 = 1.98
	assert_almost_eq(CrashRoller.crash_point_for(0.5), 1.98, 0.001)
	# r=0.0 -> 100*0.99/1.0 = 99 -> floor/100 = 0.99 -> clamp a 1.00
	assert_almost_eq(CrashRoller.crash_point_for(0.0), 1.00, 0.001)
	# r=0.9 -> 100*0.99/0.1 = 990 -> floor/100 = 9.90
	assert_almost_eq(CrashRoller.crash_point_for(0.9), 9.90, 0.001)
	# r=0.99 -> 100*0.99/0.01 = 9900 -> floor/100 = 99.00
	assert_almost_eq(CrashRoller.crash_point_for(0.99), 99.00, 0.001)

func test_roll_without_queue_never_returns_below_one():
	var roller = CrashRoller.new()
	for i in range(200):
		assert_true(roller.roll() >= 1.00)

func test_roll_distribution_median_matches_expected_house_edge():
	# Validación estadística de la fórmula pedida en la tarea del agente: no
	# basta un caso suelto, hay que ver la forma de la distribución sobre
	# muchas rondas. r es uniforme en [0,1); la mediana de crash_point debe
	# rondar 1.98x (r=0.5), y la fracción de rondas por debajo de 2x debe
	# rondar el 50% dentro de un margen estadístico razonable.
	var below_2x := 0
	var total := 5000
	var sum_min := INF
	for i in range(total):
		var cp: float = CrashRoller.crash_point_for(randf())
		sum_min = min(sum_min, cp)
		if cp < 2.0:
			below_2x += 1
	var ratio := float(below_2x) / float(total)
	assert_true(ratio > 0.45 and ratio < 0.55, "ratio bajo 2x fue %f, se esperaba ~0.5" % ratio)
	assert_true(sum_min >= 1.00)
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`
Expected: FAIL — `CrashRoller` no existe todavía.

- [ ] **Step 3: Implement CrashRoller**

```gdscript
# scripts/crash/crash_roller.gd
class_name CrashRoller
extends RefCounted

var results: Array[float] = []

func roll() -> float:
	if not results.is_empty():
		return results.pop_front()
	var r := randf()
	while r <= 0.0:
		r = randf()
	return crash_point_for(r)

static func crash_point_for(r: float) -> float:
	return max(1.00, floor(100.0 * 0.99 / (1.0 - r)) / 100.0)
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`
Expected: PASS (todos los tests de `test_crash_roller.gd`)

- [ ] **Step 5: Commit**

```bash
git add scripts/crash/crash_roller.gd tests/unit/test_crash_roller.gd
git commit -m "feat(crash): add CrashRoller with crash-point formula"
```

---

## Task 2: CrashTableState — lógica de ronda independiente por jugador

**Files:**
- Create: `scripts/crash/crash_table_state.gd`
- Test: `tests/unit/test_crash_table_state.gd`

**Interfaces:**
- Consumes: `CrashRoller` (Task 1) — `roller.roll() -> float`, `roller.results: Array[float]`.
  `ChipLedger` (`scripts/chip_ledger.gd`, ya existe) — `ChipLedger.new(starting_balance)`,
  `place_bet(amount) -> bool`, `payout(amount) -> void`, `balance: int`.
- Produces: `class_name CrashTableState extends RefCounted`.
  - `signal chips_won(player_id: int, amount: int)`
  - `const STARTING_BALANCE := 500`
  - `const GROWTH_RATE := 0.02`
  - `class Player`: `player_id: int`, `ledger: ChipLedger`, `bet_amount: int`, `crash_point:
    float`, `elapsed: float`, `is_active: bool`, `last_round: Dictionary`.
  - `players: Dictionary` (`player_id -> Player`)
  - `roller: CrashRoller`
  - `_init(p_roller: CrashRoller = null)`
  - `static func multiplier_at(t: float) -> float`
  - `func place_bet(player_id: int, amount: int) -> bool`
  - `func cash_out(player_id: int) -> bool`
  - `func advance_time(delta: float) -> Array` (devuelve los `player_id` cuya ronda explotó en
    esta llamada — lo consume `CrashTableController` en Task 3 para saber cuándo retransmitir)
  - `func to_dict() -> Dictionary`

- [ ] **Step 1: Write the failing tests**

```gdscript
# tests/unit/test_crash_table_state.gd
extends GutTest

func test_players_starts_empty():
	var table = CrashTableState.new()
	assert_eq(table.players.size(), 0)

func test_multiplier_at_matches_formula():
	assert_almost_eq(CrashTableState.multiplier_at(0.0), 1.00, 0.001)
	# 1.00 + 0.02 * 7.0710678^2 ~= 2.00
	assert_almost_eq(CrashTableState.multiplier_at(7.0710678), 2.00, 0.01)

func test_place_bet_creates_player_lazily_with_starting_balance():
	var roller = CrashRoller.new()
	roller.results = [2.00]
	var table = CrashTableState.new(roller)
	var ok = table.place_bet(111, 100)
	assert_true(ok)
	assert_true(table.players.has(111))
	assert_eq(table.players[111].ledger.balance, 400)
	assert_true(table.players[111].is_active)

func test_place_bet_fails_on_insufficient_balance():
	var table = CrashTableState.new()
	var ok = table.place_bet(111, 5000)
	assert_false(ok)
	# _player_for crea la entrada perezosa (balance 500) antes del place_bet;
	# el jugador queda creado pero intacto, no se descuenta.
	assert_eq(table.players[111].ledger.balance, 500)
	assert_false(table.players[111].is_active)

func test_place_bet_fails_if_round_already_active():
	var roller = CrashRoller.new()
	roller.results = [2.00, 3.00]
	var table = CrashTableState.new(roller)
	table.place_bet(111, 100)
	var ok = table.place_bet(111, 50)
	assert_false(ok)
	# no se descontó la segunda apuesta
	assert_eq(table.players[111].ledger.balance, 400)

func test_cash_out_before_crash_pays_bet_times_current_multiplier():
	var roller = CrashRoller.new()
	roller.results = [5.00] # explota en 5x, mucho más tarde
	var table = CrashTableState.new(roller)
	table.place_bet(111, 100)
	table.advance_time(7.0710678) # multiplicador actual ~2.00x
	var ok = table.cash_out(111)
	assert_true(ok)
	# payout = int(100 * 2.00) = 200
	assert_eq(table.players[111].ledger.balance, 500 - 100 + 200)
	assert_false(table.players[111].is_active)

func test_cash_out_emits_chips_won_with_net_profit():
	var roller = CrashRoller.new()
	roller.results = [5.00]
	var table = CrashTableState.new(roller)
	watch_signals(table)
	table.place_bet(111, 100)
	table.advance_time(7.0710678)
	table.cash_out(111)
	assert_signal_emitted_with_parameters(table, "chips_won", [111, 100]) # 200 payout - 100 apuesta

func test_cash_out_fails_if_no_active_round():
	var table = CrashTableState.new()
	assert_false(table.cash_out(111))

func test_advance_time_resolves_loss_when_multiplier_reaches_crash_point():
	var roller = CrashRoller.new()
	roller.results = [2.00] # explota justo en 2x
	var table = CrashTableState.new(roller)
	watch_signals(table)
	table.place_bet(111, 100)
	var crashed = table.advance_time(7.0710678) # multiplicador llega a ~2.00x
	assert_eq(crashed, [111])
	assert_false(table.players[111].is_active)
	assert_eq(table.players[111].ledger.balance, 400) # perdió la apuesta
	assert_signal_not_emitted(table, "chips_won")

func test_cash_out_fails_after_round_already_crashed():
	var roller = CrashRoller.new()
	roller.results = [2.00]
	var table = CrashTableState.new(roller)
	table.place_bet(111, 100)
	table.advance_time(7.0710678)
	assert_false(table.cash_out(111)) # ya se resolvió como pérdida

func test_rounds_are_independent_per_player():
	var roller = CrashRoller.new()
	roller.results = [2.00, 10.00]
	var table = CrashTableState.new(roller)
	table.place_bet(111, 100) # explota en 2x
	table.place_bet(222, 100) # explota en 10x
	table.advance_time(7.0710678) # ~2.00x: 111 explota, 222 sigue activo
	assert_false(table.players[111].is_active)
	assert_true(table.players[222].is_active)

func test_to_dict_hides_crash_point_of_active_round():
	var roller = CrashRoller.new()
	roller.results = [3.00]
	var table = CrashTableState.new(roller)
	table.place_bet(111, 100)
	var data = table.to_dict()
	assert_true(data["players"][111]["is_active"])
	assert_false(data["players"][111].has("crash_point"))
	assert_eq(data["players"][111]["last_round"], {})

func test_to_dict_reveals_crash_point_after_round_resolves():
	var roller = CrashRoller.new()
	roller.results = [2.00]
	var table = CrashTableState.new(roller)
	table.place_bet(111, 100)
	table.advance_time(7.0710678)
	var data = table.to_dict()
	assert_false(data["players"][111]["is_active"])
	assert_eq(data["players"][111]["last_round"]["crash_point"], 2.00)
	assert_eq(data["players"][111]["last_round"]["win"], false)
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`
Expected: FAIL — `CrashTableState` no existe todavía.

- [ ] **Step 3: Implement CrashTableState**

```gdscript
# scripts/crash/crash_table_state.gd
class_name CrashTableState
extends RefCounted

signal chips_won(player_id: int, amount: int)

const STARTING_BALANCE := 500
# multiplicador(t) = 1 + GROWTH_RATE * t^2. En t=7.07s (mediana del punto de
# explosión, r=0.5 -> 1.98x) el multiplicador llega a ~2.00x, en medio del
# rango de 3-15s pedido por la spec para una ronda "típica". Ver plan
# docs/superpowers/plans/2026-08-20-crash.md para el resto de la cuenta.
const GROWTH_RATE := 0.02

class Player:
	var player_id: int = 0
	var ledger: ChipLedger
	var bet_amount: int = 0
	var crash_point: float = 0.0
	var elapsed: float = 0.0
	var is_active: bool = false
	var last_round: Dictionary = {}

var players: Dictionary = {}
var roller: CrashRoller

func _init(p_roller: CrashRoller = null) -> void:
	roller = p_roller if p_roller else CrashRoller.new()

static func multiplier_at(t: float) -> float:
	return 1.0 + GROWTH_RATE * t * t

func _player_for(player_id: int) -> Player:
	if not players.has(player_id):
		var player := Player.new()
		player.player_id = player_id
		player.ledger = ChipLedger.new(STARTING_BALANCE)
		players[player_id] = player
	return players[player_id]

func place_bet(player_id: int, amount: int) -> bool:
	var player := _player_for(player_id)
	if player.is_active:
		return false
	if not player.ledger.place_bet(amount):
		return false
	player.bet_amount = amount
	player.crash_point = roller.roll()
	player.elapsed = 0.0
	player.is_active = true
	return true

func cash_out(player_id: int) -> bool:
	if not players.has(player_id):
		return false
	var player: Player = players[player_id]
	if not player.is_active:
		return false
	var current := multiplier_at(player.elapsed)
	if current >= player.crash_point:
		return false
	var payout := int(player.bet_amount * current)
	player.ledger.payout(payout)
	chips_won.emit(player_id, payout - player.bet_amount)
	player.last_round = {
		"bet_amount": player.bet_amount,
		"crash_point": player.crash_point,
		"cashed_out_at": current,
		"win": true,
		"payout": payout,
	}
	player.is_active = false
	return true

# Llamar cada frame desde el host (CrashTableController._process). Devuelve
# los player_id cuya ronda acaba de explotar en esta llamada, para que el
# controller sepa cuándo retransmitir el estado sin tener que hacerlo cada
# frame para todo el mundo.
func advance_time(delta: float) -> Array:
	var crashed: Array = []
	for player_id in players:
		var player: Player = players[player_id]
		if not player.is_active:
			continue
		player.elapsed += delta
		if multiplier_at(player.elapsed) >= player.crash_point:
			player.last_round = {
				"bet_amount": player.bet_amount,
				"crash_point": player.crash_point,
				"cashed_out_at": -1.0,
				"win": false,
				"payout": 0,
			}
			player.is_active = false
			crashed.append(player_id)
	return crashed

func to_dict() -> Dictionary:
	var players_data := {}
	for player_id in players:
		var player: Player = players[player_id]
		players_data[player_id] = {
			"player_id": player.player_id,
			"balance": player.ledger.balance,
			"is_active": player.is_active,
			"elapsed": player.elapsed,
			"last_round": player.last_round,
		}
	return {"players": players_data}
```

`crash_point` de una ronda activa **no** se incluye en `to_dict()` — sólo vive en
`last_round` una vez resuelta la ronda (`cash_out` o `advance_time`). Esto es intencional
(ver "Decisión de diseño: qué ve el cliente" arriba): filtrar el punto de explosión mientras
la ronda sigue en curso dejaría a un cliente hostil retirarse justo antes de explotar.

- [ ] **Step 4: Run tests to verify they pass**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`
Expected: PASS (todos los tests de `test_crash_table_state.gd`, y los de Task 1 siguen en verde)

- [ ] **Step 5: Commit**

```bash
git add scripts/crash/crash_table_state.gd tests/unit/test_crash_table_state.gd
git commit -m "feat(crash): add CrashTableState with independent per-player rounds"
```

---

## Task 3: CrashTableController — wrapper de red host-vs-cliente

**Files:**
- Create: `scripts/net/crash_table_controller.gd`

**Interfaces:**
- Consumes: `CrashTableState` (Task 2) — `place_bet`, `cash_out`, `advance_time`, `to_dict`,
  señal `chips_won`.
- Produces: `class_name CrashTableController extends Node`.
  - `signal state_changed(state: Dictionary)`
  - `signal chips_won(player_id: int, amount: int)`
  - `var table_state: CrashTableState`
  - `func place_bet(amount: int) -> void` (entrada pública de UI)
  - `func cash_out() -> void` (entrada pública de UI)
  - `func request_state() -> void`

No hay test GUT dedicado para esta capa — igual que `dice_table_controller.gd`,
`roulette_table_controller.gd` y el resto de `scripts/net/*_controller.gd`, ninguno tiene test
unitario porque dependen de `MultiplayerAPI`/RPCs reales; la verificación de esta capa es manual
(Task 5, arrancar dos instancias) y por lectura/paridad estructural con `DiceTableController`.

- [ ] **Step 1: Implement CrashTableController**

```gdscript
# scripts/net/crash_table_controller.gd
class_name CrashTableController
extends Node

signal state_changed(state: Dictionary)
signal chips_won(player_id: int, amount: int)

var table_state: CrashTableState

func _ready() -> void:
	if multiplayer.is_server():
		table_state = CrashTableState.new()
		table_state.chips_won.connect(_on_table_chips_won)

# Sólo el host hace avanzar el tiempo de las rondas activas — es la única
# autoridad sobre cuándo explota cada una. Sólo se retransmite el estado
# cuando alguna ronda acaba de explotar (no cada frame): el resto del tiempo
# los clientes extrapolan localmente el multiplicador desde el último
# broadcast, más barato en ancho de banda.
func _process(delta: float) -> void:
	if table_state == null or not multiplayer.is_server():
		return
	var crashed := table_state.advance_time(delta)
	if not crashed.is_empty():
		_broadcast_state()

# Puntos de entrada para la UI local (host o cliente). El host actúa directo
# sobre table_state; un rpc_id(1, ...) a sí mismo lo rechaza el MultiplayerAPI
# ("call_remote" no permite invocarse a uno mismo), así que el host no puede
# pasar por las RPCs request_* para sus propias acciones.
func place_bet(amount: int) -> void:
	if multiplayer.is_server():
		_apply_place_bet(amount, multiplayer.get_unique_id())
	else:
		request_place_bet.rpc_id(1, amount)

func cash_out() -> void:
	if multiplayer.is_server():
		_apply_cash_out(multiplayer.get_unique_id())
	else:
		request_cash_out.rpc_id(1)

# Un cliente que entra a la mesa después de que ya hubo rondas no recibe nada
# por su cuenta: el host solo retransmite _receive_state cuando algo cambia,
# nunca al conectar (mismo gotcha que TableController.request_state en Plan
# 3). Sin este pedido explícito el cliente se queda con el estado en blanco
# para siempre.
func request_state() -> void:
	if multiplayer.is_server():
		return
	_request_state.rpc_id(1)

@rpc("any_peer", "call_remote", "reliable")
func _request_state() -> void:
	if not multiplayer.is_server():
		return
	_receive_state.rpc_id(multiplayer.get_remote_sender_id(), table_state.to_dict())

@rpc("any_peer", "call_remote", "reliable")
func request_place_bet(amount: int) -> void:
	if not multiplayer.is_server():
		return
	_apply_place_bet(amount, multiplayer.get_remote_sender_id())

@rpc("any_peer", "call_remote", "reliable")
func request_cash_out() -> void:
	if not multiplayer.is_server():
		return
	_apply_cash_out(multiplayer.get_remote_sender_id())

func _apply_place_bet(amount: int, player_id: int) -> void:
	if table_state.place_bet(player_id, amount):
		_broadcast_state()

func _apply_cash_out(player_id: int) -> void:
	if table_state.cash_out(player_id):
		_broadcast_state()

func _broadcast_state() -> void:
	_receive_state.rpc(table_state.to_dict())

# Broadcast a TODOS los presentes en CasinoFloor, no solo a quien actuó — la
# spec pide visibilidad compartida de las rondas de los demás aunque nadie
# más participe en ellas.
@rpc("authority", "call_local", "reliable")
func _receive_state(state: Dictionary) -> void:
	state_changed.emit(state)

func _on_table_chips_won(player_id: int, amount: int) -> void:
	chips_won.emit(player_id, amount)
```

- [ ] **Step 2: Run full unit suite to make sure nothing broke**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`
Expected: PASS (esta capa no añade tests GUT nuevos, pero confirma que Tasks 1-2 siguen verdes)

- [ ] **Step 3: Commit**

```bash
git add scripts/net/crash_table_controller.gd
git commit -m "feat(crash): add CrashTableController network wrapper"
```

---

## Task 4: UI de la mesa — `crash_table_net.tscn`

**Files:**
- Create: `scenes/crash_table_net.gd`
- Create: `scenes/crash_table_net.tscn`

**Interfaces:**
- Consumes: `CrashTableController` (Task 3) — `place_bet(amount)`, `cash_out()`,
  `request_state()`, señal `state_changed(state)`. `CrashTableState.multiplier_at(t)` (Task 2,
  estático, sin necesidad de instancia) para calcular el número que se muestra en pantalla.
  `NetworkManager.peer_steam_ids` / `identities_changed` (ya existen, mismo uso que
  `dice_table_net.gd`).

- [ ] **Step 1: Implement the scene script**

```gdscript
# scenes/crash_table_net.gd
extends Control

@onready var table_controller: CrashTableController = $TableController
@onready var players_label: Label = $PlayersLabel
@onready var amount_spinbox: SpinBox = $AmountSpinBox
@onready var bet_button: Button = $BetButton
@onready var cash_out_button: Button = $CashOutButton

var _last_players: Dictionary = {}
var _local_elapsed: Dictionary = {} # player_id -> float, extrapolación local desde el último broadcast

func _display_name(peer_id: int) -> String:
	var steam_id: int = NetworkManager.peer_steam_ids.get(peer_id, 0)
	if steam_id == 0:
		return "jugador %d" % peer_id
	var persona_name := Steam.getFriendPersonaName(steam_id)
	return persona_name if not persona_name.is_empty() else "jugador %d" % peer_id

func _ready() -> void:
	table_controller.state_changed.connect(_on_state_changed)
	bet_button.pressed.connect(func(): table_controller.place_bet(int(amount_spinbox.value)))
	cash_out_button.pressed.connect(func(): table_controller.cash_out())
	NetworkManager.identities_changed.connect(_refresh_players_label)
	cash_out_button.disabled = true
	# Mismo gotcha que blackjack_table_net.gd/roulette_table_net.gd/dice_table_net.gd: un
	# cliente recién llegado a esta escena puede que aún no tenga el
	# SteamMultiplayerPeer en CONNECTED — bloquear la apuesta hasta entonces evita el
	# rpc_id() fallido.
	if not multiplayer.is_server():
		var peer := multiplayer.multiplayer_peer
		if peer == null or peer.get_connection_status() != MultiplayerPeer.CONNECTION_CONNECTED:
			bet_button.disabled = true
			multiplayer.connected_to_server.connect(func():
				bet_button.disabled = false
				table_controller.request_state()
			)
		else:
			table_controller.request_state()

func _process(delta: float) -> void:
	var any_active := false
	for player_id in _last_players:
		if _last_players[player_id]["is_active"]:
			_local_elapsed[player_id] = _local_elapsed.get(player_id, 0.0) + delta
			any_active = true
	if any_active:
		_refresh_players_label()

func _on_state_changed(state: Dictionary) -> void:
	_last_players = state["players"]
	for player_id in _last_players:
		_local_elapsed[player_id] = _last_players[player_id]["elapsed"]
	var my_id := multiplayer.get_unique_id()
	var mine: Dictionary = _last_players.get(my_id, {})
	var mine_active: bool = mine.get("is_active", false)
	cash_out_button.disabled = not mine_active
	if not (not multiplayer.is_server() and multiplayer.multiplayer_peer != null and multiplayer.multiplayer_peer.get_connection_status() != MultiplayerPeer.CONNECTION_CONNECTED):
		bet_button.disabled = mine_active
	_refresh_players_label()

func _refresh_players_label() -> void:
	if _last_players.is_empty():
		players_label.text = "Nadie ha apostado todavía."
		return
	var lines: Array[String] = []
	for player_id in _last_players:
		var player = _last_players[player_id]
		var line := "%s — fichas %d" % [_display_name(player["player_id"]), player["balance"]]
		if player["is_active"]:
			var t: float = _local_elapsed.get(player_id, player["elapsed"])
			var current := CrashTableState.multiplier_at(t)
			line += " — en juego, multiplicador %.2fx" % current
		else:
			var last_round = player["last_round"]
			if not last_round.is_empty():
				if last_round["win"]:
					line += " — última ronda: retiró en %.2fx" % last_round["cashed_out_at"]
				else:
					line += " — última ronda: explotó en %.2fx" % last_round["crash_point"]
		lines.append(line)
	players_label.text = "\n".join(lines)
```

- [ ] **Step 2: Create the scene**

```
# scenes/crash_table_net.tscn
[gd_scene load_steps=3 format=3]

[ext_resource type="Script" path="res://scenes/crash_table_net.gd" id="1"]
[ext_resource type="Script" path="res://scripts/net/crash_table_controller.gd" id="2"]

[node name="CrashTableNet" type="Control"]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
script = ExtResource("1")

[node name="TableController" type="Node" parent="."]
script = ExtResource("2")

[node name="PlayersLabel" type="Label" parent="."]
layout_mode = 0
offset_left = 20.0
offset_top = 20.0
offset_right = 700.0
offset_bottom = 140.0
text = "Nadie ha apostado todavía."

[node name="AmountSpinBox" type="SpinBox" parent="."]
layout_mode = 0
offset_left = 20.0
offset_top = 160.0
offset_right = 160.0
offset_bottom = 196.0
min_value = 1.0
max_value = 500.0
step = 10.0
value = 50.0

[node name="BetButton" type="Button" parent="."]
layout_mode = 0
offset_left = 180.0
offset_top = 160.0
offset_right = 420.0
offset_bottom = 196.0
text = "Apostar"

[node name="CashOutButton" type="Button" parent="."]
layout_mode = 0
offset_left = 440.0
offset_top = 160.0
offset_right = 680.0
offset_bottom = 196.0
text = "Retirar"
```

Anchos de `BetButton`/`CashOutButton` (240px cada uno) son generosamente mayores que el ancho
real de "Apostar"/"Retirar" en español con la fuente del proyecto — mismo margen de seguridad
que `BetOverButton`/`BetUnderButton` en `dice_table_net.tscn` (240px para textos más largos como
"Apostar Mayor que"), evita el gotcha de Ruleta (`cb50b8e`) de anchos fijos genéricos que
recortan el texto.

- [ ] **Step 3: Manual verification (host + 1 cliente)**

No hay test automatizado de escena. Verificación manual mínima antes de dar la tarea por
terminada:
1. Arrancar el proyecto en dos instancias de Godot (host y cliente) contra el mismo lobby de
   Steam, entrar a `CasinoFloor`.
2. Host pulsa "Apostar" en la mesa de Crash: el número sube visiblemente en su propia UI y
   también en la del cliente (visibilidad compartida).
3. Host pulsa "Retirar" antes de que explote: el saldo sube según el multiplicador mostrado en
   ese instante, y ambas UIs reflejan "retiró en X.XXx".
4. Repetir dejando que explote sin retirar: el saldo baja el importe apostado, ambas UIs
   reflejan "explotó en X.XXx" con el mismo valor.
5. Cliente entra a mitad de una ronda ajena ya en marcha del host: tras `request_state()` ve el
   multiplicador del host avanzando (no en blanco).

- [ ] **Step 4: Commit**

```bash
git add scenes/crash_table_net.gd scenes/crash_table_net.tscn
git commit -m "feat(crash): add CrashTableNet scene UI"
```

---

## Task 5: Integración en `CasinoFloor`

**Files:**
- Modify: `scenes/casino_floor.tscn`
- Modify: `scripts/net/casino_floor.gd`

**Interfaces:**
- Consumes: `CrashTableNet` (Task 4, PackedScene), `CrashTableController.chips_won` (Task 3).

`DiceTableNet` termina en `offset_bottom = 1400`, y justo debajo empiezan `GoalLabel` (1420),
`UnlockedBanner` (1456) y `BattleStatusLabel` (1420-1490). Se inserta la franja de Crash en ese
hueco (1420-1720) y se desplazan las tres etiquetas 300px hacia abajo para que sigan yendo al
final. **Aviso para la sesión pilar:** los Agentes 10 (Mines) y 11 (Plinko) arrancan del mismo
`main` en paralelo y muy probablemente also van a insertar su franja justo encima de esas mismas
etiquetas — es casi seguro que haya conflicto de merge en `casino_floor.tscn` (y en el bucle
`find_children` de `casino_floor.gd`) entre los tres; toca resolverlo a mano al integrar, no es un
error de ninguno de los tres agentes.

- [ ] **Step 1: Update casino_floor.tscn**

Añadir el `ext_resource` de `crash_table_net.tscn`, el nodo `CrashTableNet` en el hueco
1420-1720, y desplazar `GoalLabel`/`UnlockedBanner`/`BattleStatusLabel` 300px hacia abajo:

```
# añadir junto a los demás ext_resource, con el índice load_steps actualizado (7 -> 8)
[ext_resource type="PackedScene" path="res://scenes/crash_table_net.tscn" id="7"]
```

```
# nuevo nodo, insertado después de DiceTableNet
[node name="CrashTableNet" parent="." instance=ExtResource("7")]
anchor_bottom = 0.0
offset_top = 1420.0
offset_bottom = 1720.0
```

```
# GoalLabel, UnlockedBanner y BattleStatusLabel: sumar 300 a cada offset_top/offset_bottom
[node name="GoalLabel" type="Label" parent="."]
offset_left = 20.0
offset_top = 1720.0
offset_right = 700.0
offset_bottom = 1744.0
text = "Meta colectiva: 0 / 1000 fichas"

[node name="UnlockedBanner" type="Label" parent="."]
offset_left = 20.0
offset_top = 1756.0
offset_right = 700.0
offset_bottom = 1790.0
text = "¡Meta colectiva alcanzada! Nueva mesa desbloqueada."
visible = false

[node name="BattleStatusLabel" type="Label" parent="."]
offset_left = 20.0
offset_top = 1720.0
offset_right = 700.0
offset_bottom = 1790.0
text = "Equipos: —"
```

- [ ] **Step 2: Connect CrashTableController.chips_won in free mode**

En `scripts/net/casino_floor.gd`, junto al bucle existente de `DiceTableController` (líneas
34-35), añadir:

```gdscript
			for controller in find_children("*", "DiceTableController", true, false):
				controller.chips_won.connect(_on_chips_won)
			for controller in find_children("*", "CrashTableController", true, false):
				controller.chips_won.connect(_on_chips_won)
```

- [ ] **Step 3: Run full unit suite**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/unit -gexit`
Expected: PASS (sin regresiones — este task no toca lógica pura, sólo escena/conexión)

- [ ] **Step 4: Commit**

```bash
git add scenes/casino_floor.tscn scripts/net/casino_floor.gd
git commit -m "feat(crash): integrate CrashTableNet into CasinoFloor"
```

- [ ] **Step 5: Push and report to pilar**

```bash
git push -u origin feature/crash
```

Informar a la sesión pilar con: archivos tocados (sobre todo `casino_floor.tscn`/`casino_floor.gd`
para anticipar el conflicto con Mines/Plinko avisado arriba), el `growth_rate = 0.02` elegido y su
justificación (sección "Decisión de diseño: growth_rate"), y cómo se verificó la fórmula del punto
de explosión — test estadístico sobre 5000 rondas simuladas en
`test_crash_roller.gd::test_roll_distribution_median_matches_expected_house_edge`, no sólo casos
sueltos.
